<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.iverson</groupId>
  <artifactId>iverson-client-java</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Iverson Java Client</name>
  <description>Java gRPC client for the Iverson entity store</description>

  <modules>
    <module>client</module>
    <module>sample</module>
    <module>conformance</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <grpc.version>1.71.0</grpc.version>
    <protobuf.version>4.30.2</protobuf.version>
    <junit.version>5.11.0</junit.version>
    <mockito.version>5.23.0</mockito.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.grpc</groupId>
        <artifactId>grpc-bom</artifactId>
        <version>${grpc.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-bom</artifactId>
        <version>${protobuf.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <!--
          Pinned explicitly (CSR round-5 finding #3 follow-up): CI invokes this
          plugin as a bare `org.owasp:dependency-check-maven:check` goal with no
          version qualifier, which floats to whatever is newest at build time.
          Confirmed empirically it currently resolves to 13.0.0 — pin it so a
          future plugin release can't silently change CI's gating behavior.
        -->
        <plugin>
          <groupId>org.owasp</groupId>
          <artifactId>dependency-check-maven</artifactId>
          <version>13.0.0</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
