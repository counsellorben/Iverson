# Critical Implementation Review: 2026-09-04-multivector-experiment-implementation-plan (Round 3)

**Plan:** `/home/ben/repositories/Iverson/docs/plans/2026-09-04-multivector-experiment-implementation-plan.md`
**Verified plan-level assumptions section:** present

⚠️ 53 commits since plan-write time (SHA `5563454`); cited file:line references re-checked under §1.

The two commits added since round 2 are `b245365` (that review) and `65c0b25` (its seven applied fixes). The plan's runtime — `main` at `65c0b25` — is otherwise the same tree round 2 read, so this round's search concentrated on the surfaces the fix rewrote (Task 1 step 2, Task 4 step 1, Task 5 steps 1/3/4/5) and on the surfaces both prior rounds passed with an `ok` that named a shape rather than an exact string.

Evidence base: `main` at `65c0b25` read directly; the spec at `d678dbe`; the Phase 1 and Phase 2 migration plans read at their cited lines; the live stack read but never written — `docker ps`, `docker compose config` / `config --services` (plain and `--profile tei`), Qdrant collection info, an exact `points/count`, one payload-only scroll and one named-vector search, TEI `/info`, Ollama `/api/tags`, the API `/build` endpoint, one `psql -tAc` select, one `docker exec … du -sh`; `ingest.py` imported under a socket guard; `ir_measures` exercised read-only; `~/repositories/iverson-benchmark-corpora/` listed and two on-disk sidecars read. No container, collection, table, or repository file was written except this review. `stack.py`, `docker compose up`, `ingest.py` and `dotnet run` were not run.

## 0. Coverage enumeration

### Tasks × surfaces

| Row | Surface | Disposition |
|---|---|---|
| T1-target | Task 1 **Files** header, File-Structure entry and step 1's "Replace line 177" | ok — `docker-compose.yml:177` is byte-exactly the quoted `command:` line; `:175` `container_name: iverson-tei-embed`, `:176` `restart: unless-stopped`, `:173` the service key. Round 2's repoint landed on the right line |
| T1-text | Task 1 step 1 before/after block | ok — the "after" line keeps `${EMBED_MODEL_ID:-BAAI/bge-base-en-v1.5}` and appends `"--max-batch-tokens", "${TEI_MAX_BATCH_TOKENS:-16384}"`, which is spec §3.2's default-preserving shape verbatim; the three comment lines above it name the spec row that forced it |
| T1-verify-1 | Task 1 step 2 first pipeline (`docker compose config \| grep -A 9 '^  tei-embed:' \| grep -A 5 'command:'`) | ok — rendered the live config this round: without `profiles:` the service emits `command:` as its **first** key, so lines 2–6 of the `-A 9` window are the command items; with two items appended the window still spans all six command lines and the inner `-A 5` prints exactly the five items ending `- --max-batch-tokens` / `- "16384"` |
| T1-verify-2 | Task 1 step 2 second pipeline (`TEI_MAX_BATCH_TOKENS=4096 … \| grep -c`) | ok — the alternation `'"4096"\|^      - 4096$'` covers both renderings; compose indents command items at six spaces, confirmed in the live render |
| T1-verify-3 | Task 1 step 2 third command (`config --services \| grep -c '^tei-embed$'   # 1`) | ok — ran live: prints **1**. Round 2's §2.1 fix corrected both the command and the annotation |
| T1-commit | Task 1 step 3 | ok — tracked file; `compose:`-prefixed lowercase imperative subject matches `git log --oneline -8` |
| T2-tests | Task 2 step 1 test file (10 tests) | ok — `ingest.key_to_ulong("29beccc1-3d1c-577b-a9a2-114337e70200")` → `817174487868073` re-derived live, matching the file's `OID`; `DocumentRanking.CollapseByDocId` (`DocumentRanking.cs:28-45`) is max-per-doc / `OrderByDescending` (stable) / `Take(limit)`, which is what `test_collapse_*` pins, ties included; `TrecRunWriter.cs:31` is `{queryId} Q0 {docId} {rank} {score:F6} {runTag}`, which is what `test_trec_lines_match_trecrunwriter_format` pins |
| T2-red | Task 2 step 2 expected `ModuleNotFoundError` | ok — `ls Iverson.Server/Iverson.LoadTest/scripts/` on `main`: no `multivector.py`, no `test_multivector.py` (P1) |
| T2-code | Task 2 step 3 `multivector.py` | ok — every `ingest` symbol it names resolves under a socket-guarded import: `qdrant_request(method, path, body=None)` `:279`, `key_to_ulong(key: str) -> int` `:229`, `embed(text, model, document_prefix, embed_url)` `:533`, `DEFAULT_OBJECT_COLLECTION`/`DEFAULT_CHUNKS_COLLECTION` `:182/:185`, `QDRANT_URL` `:148`, `HTTP_TIMEOUT_SECONDS` `:157`, `EMBED_RETRIES` `:158`. No phantom names remain in the block: `argparse/json/os/subprocess/sys/time` are each used (`subprocess` only in `disk_size`, `time` only in `wait_for_index`), and all fourteen module-level constants are referenced. `chunks_info["config"]["params"]["vectors"]["body_vector"]["size"]` resolves live to 768 |
| T2-build-dyn | `cmd_build` runtime path | ok — order is create → `load_objects` → `group_rows(scroll(...))` → `resolve_parents` → batched upsert (`wait=false` … `wait=true` on the last) → rows reconciliation → `points_count` reconciliation → `wait_for_index`. `resolve_parents` re-uses the row lists rather than copying, so peak memory stays at the ~0.5 GB the comment claims; a 100-point batch serialises to ~6 MB, well under Qdrant's request cap and inside `HTTP_TIMEOUT_SECONDS` 300 |
| T2-index | `wait_for_index` (`green` **and** `indexed_vectors_count > 0`) | ok on its cited evidence — re-derived the arithmetic: `indexing_threshold` is 10,000 KB (live `optimizer_config`), the chunks collection holds 19,967 × 768 × 4 B ≈ 61 MB across 2 segments (~30 MB each) and *is* indexed (16,678 / 19,967), so a single-storage collection of the same size clears the threshold. See the dropped row D5 for the part of this the arithmetic does not reach |
| T2-smoke | Task 2 step 5 smoke against scratch collections | ok — reads the live chunks/object collections (still 19,967 / 5,183 at 768) and writes only `scratch_multivector_smoke` and `/tmp/mv-smoke`, both deleted in-step; the api-key literal matches compose's `QDRANT__SERVICE__API_KEY`; the second `build` exits 1 via `sys.exit(str)` as annotated |
| T2-commit | Task 2 step 6 | ok |
| T3-tests | Task 3 step 1 (two `rank_chunk_hits` tests) | ok — assertions match the function as written; `SystemExit` on the unknown parent |
| T3-red | Task 3 step 2 expected `AttributeError` | ok |
| T3-code | Task 3 step 3 `rank_chunk_hits` + `cmd_query` + subparser | ok — the per-chunk body `{vector:{name:"body_vector",vector}, limit:250, with_payload:["parent_id"]}` was issued read-only against the live chunks collection this round: `result` is a **list** of 250 objects with keys `id/payload/score/version`, spanning 204 distinct `parent_id`s, so both the shape `rank_chunk_hits` unpacks and the ≥ 50-document fill the under-fill guard demands hold. The `try/finally write_outputs()` path still writes both `.trec` files and the sidecar on every exit; `index_state` and the green precondition (CIR-1 §2.2's fix) are present. The sidecar's `chunks`/`multivector` latency keys do not collide with its `chunks_collection`/`multivector_collection` keys |
| T3-embed | `ingest.embed(text, args.model, "", args.embed_url)` | ok — `ingest.py:533-557` posts `{model, input}` to `<embed_url>/v1/embeddings` and returns `data[0]["embedding"]`; `ingest.document_prefix_for("Alibaba-NLP/gte-modernbert-base")` → `''` and `verify_contract` passes, both re-run under the socket guard |
| T3-smoke | Task 3 step 5 smoke (nomic via Ollama on 11434) | dropped — see D1. Mechanically sound: `nomic-embed-text:latest` is still in the Ollama volume (`/api/tags`, live) and is 768-dim like the live bge-base chunk vectors, so the scratch collection accepts the query vector |
| T3-commit | Task 3 step 6 | ok |
| T4-pre | Task 4 step 1 preconditions | → §2.2 for the closing sentence. The checks themselves are ok and were run read-only: the count/size greps print `"points_count":19967` / `"size":768` / `"points_count":5183`; `/build` is compact JSON so `grep -o '"composite":"[^"]*"'` matches there; `/info` → `BAAI/bge-base-en-v1.5`; `scifact-bge-base-qdrant-snapshots/` holds exactly 2 `.snapshot` files plus `RESTORE.md`; all six health-loop containers and all six `--dry-run` service names (`qdrant ollama postgres redis authentik-server iverson-api`) exist in `docker compose config --services` |
| T4-tei | Task 4 step 2 run dir + TEI recreate + `/info` check | ok — `mkdir -p $MV/runs && cp -r $SCI/beir $MV/ && cp $SCI/qrels.trec $MV/` is Phase 1 `:819` with the paths substituted, and `$SCI` holds `beir/{corpus,queries}.jsonl` + `qrels.trec`; the four-value `/info` grep works (live: `model_id`, `max_input_length`, `max_batch_tokens`, `auto_truncate` all emitted, all matched). The stale `--profile tei` is D2 |
| T4-ingest | Task 4 step 3 background ingest + completion checks | ok — the flags exist; the sidecar written to `<key-map-path>.stats.json` carries exactly the keys the step's comment lists (`documents/chunks/embed_calls/embeds_saved/elapsed_seconds/model/embed_url/chunk_max_chars/chunk_step`) — read off `scifact-bge-base-2026-09-04/keymap.json.stats.json`; `verify_contract(args.model)` is the permissive call, so gte does not abort the run |
| T4-snap | Task 4 step 4 gte snapshot loop + `RESTORE.md` | ok — byte-identical to Phase 1 `:839-846`, which still sits at those lines; the snapshot-create response carries exactly one `"name":"…"`, so `grep -o … \| cut -d'"' -f4` yields the file name; the `printf` has two `%s` for two arguments and no stray `%` |
| T4-api | Task 4 step 5 schema delete + API recreate + identity check + `benchmark-query` | → §2.1 for the final composite grep. The rest is ok: `Embeddings__ModelId=${BENCH_EMBED_MODEL:-BAAI/bge-base-en-v1.5}` (`:444`) and `Embeddings__BaseUrl=http://tei-embed:80` (`:437`); the expected log text matches `EmbeddingService.cs:55`'s template exactly, prefixes empty for gte; `docker inspect … grep -o '"Embeddings__ModelId=[^"]*"'` matches the Env array's own quoting; `benchmark-query` writes `<label>.{chunks,similar}.trec` (`BenchmarkQueryScenario.cs:284-288`) at 50 documents × 300 queries |
| T4-shell | Task 4 shell state (`K`, `SCI`, `D`, `MV`, `SNAP`) | ok — each is set in-task before first use |
| T5-mv | Task 5 step 1 `export MV=$(ls -d …/scifact-gte-20*/ \| tail -1); MV=${MV%/}; ls … \|\| exit 1` | ok — the narrowed glob no longer matches `scifact-gte-qdrant-snapshots`, the trailing slash is stripped, and the guard is now `\|\|`-chained so a wrong or empty `MV` stops the step (CIR-2 §2.4's fix, correctly applied) |
| T5-run | Task 5 step 1 build → query → stats | ok — `$MV` and `K` are established in-step; `build`'s expected line names the indexed count; `query`'s ≈ 5 min estimate matches 300 embeds at ~1 s each; `stats --run-dir $MV` writes `$MV/runs/storage.json` |
| T5-snap | Task 5 step 2 multivector snapshot | ok — same loop as Task 4 step 4 against one collection; `$SNAP` was created by Task 4 step 4 |
| T5-score-a | Task 5 step 3 (a) `--run $MV/runs --stats-path …` | ok — `resolve_run_paths` (`report.py:117-153`) expands a directory to sorted `*.trec` and excludes the `--qrels` path by abspath, so only the four run files are scored; `print_stats` (`:308-340`) reads the sidecar keys the step names |
| T5-score-b | (b) two `--run` files + `--baseline gte-chunks-raw.chunks.trec` | ok — `run_paired_statistics` excludes the baseline by abspath (`:542-548`) and materialises the baseline run once before iterating measures (`:559`), so the Holm family is 2 and each measure gets its own CI line. Splitting the spec's single §6 step 7 bullet into (a) + (b) changes the family size but not any gated number |
| T5-score-c | (c) model observation vs the bge-base M1 run | ok — every path resolves: `scifact-bge-base-2026-09-04/runs/bge-base.{chunks,similar}.trec` and `.meta.json` (composite `7d3a15092f963723`, read off disk this round), `scifact-run-2026-08-26/runs/rerank-a0.{chunks,similar}.trec`; `$MV/qrels.trec` is a copy of `$SCI/qrels.trec`, which is byte-identical (`md5sum`) to `$BGE/qrels.trec`, so one `--qrels` scores all six runs. CIR-2 §2.5's fix landed correctly |
| T5-restore | Task 5 step 4 restore block (rewritten by the CIR-2 fix) | ok — the `${f%%-6802952876034638*}` derivation yields the two bge-base collection names exactly; the delete/upload/verify sequence is Phase 2 `:514-518` with the multivector delete prepended; `unset BENCH_EMBED_MODEL TEI_MAX_BATCH_TOKENS` before the TEI recreate leaves `max_batch_tokens` at the compose default 16384, which is the value the live container carries today, so the box ends as found; `docker start iverson-zookeeper iverson-kafka iverson-starrocks iverson-jaeger` plus `up -d --no-deps iverson-api iverson-worker` covers all five containers Task 4 step 1 stopped; the `SCHEMA-OK` check now runs against a live `tei-embed` |
| T5-gate | Task 5 step 5 gate-document prose | ok — the arms table now lists five arms with `bge-base` first and `rerank-a0` as context, the observation sentence names `gte-chunks-api` vs `bge-base`, and the closing box-state line is the bge-base state. Every number it asks for has a producer: CI bounds from (b), p95s and the ratio from `raw-latency.json`, points/indexed/segments/disk from `storage.json`, ingest `elapsed_seconds`/`embed_calls` from the sidecar via (a), the composite from `.meta.json`. Both named templates exist under `docs/plans/` |
| T5-commit | Task 5 step 6 `git add -f` | ok — `.gitignore:49` is `**/docs/plans/`, `git check-ignore -v` confirms the rule, and 49 files under it are tracked, so `-f` is required and sufficient |
| GC | Global Constraints | ok as executable directives — the precondition now names the bge-base baseline (live-confirmed), the window/counts/labels/run-dir are what Tasks 4–5 pass, `--pair` appears nowhere in the plan, and `git add -f` matches T5-commit. Two non-executable overstatements are dropped: D2 (the `stack.py` rationale) and D3 (`EMBED_MODEL_ID` on the API recreate) |

### Cross-task interface contracts (⧫ = persistence boundary)

| Row | Contract | Disposition |
|---|---|---|
| C1 | Task 3 consumes Task 2's `scroll`, `require_collection`, `collapse_by_doc`, `trec_lines`, `summarize_latency`, `add_common_args` | ok — all six are defined in Task 2's block with the arities Task 3 calls (`require_collection` is used by Task 3 but omitted from Task 2's *Produces* list — bookkeeping only, the function exists) |
| C2 ⧫ | `build` reads chunk payload `{parent_id, chunk_index}` + `vector.body_vector` and object payload `{key, docId}` written by `ingest.py` | ok — a live payload-only scroll returned `{"key":"d2b2694a-…","docId":"21993510"}` from the object collection and the named search returned `payload.parent_id` from the chunks collection; `key_to_ulong(key)` reproduces the live object point id exactly |
| C3 ⧫ | Task 3 `query` reads the multivector payload `docId` written by Task 2 `build` | ok — `resolve_parents` writes `{key, docId, chunk_count}`; the query asks `with_payload: ["docId"]` |
| C4 ⧫ | Task 5 consumes Task 3's two `.trec` files and `raw-latency.json` | ok — file names are the module constants plus `.chunks.trec`; the gate reads `chunks.p95_ms` / `multivector.p95_ms`, which is what `write_outputs` emits per mode, plus `index_state` |
| C5 ⧫ | Task 5 consumes Task 4's `$MV` run dir, labels and `keymap.json.stats.json` | ok now — the parameter is sourced by the narrowed dated glob and proven by `gte-chunks-api.meta.json`, with a blocking guard. This is the eval-side call site round 2 flagged; its inputs now come from an artifact only Task 4 can have written |
| C6 ⧫ | Task 5 step 3(c) consumes two pre-existing run directories as observation and context | ok — all four files plus both `.meta.json` sidecars exist on disk; the qrels files are byte-identical, which is what makes one `--qrels` legitimate across three run dirs |
| C7 ⧫ | `report.py` / `ir_measures` consume the raw TREC files | ok — re-ran `ir_measures.read_trec_run` on the plan's sample line → `ScoredDoc(query_id='1', doc_id='15830352', score=0.812345)` |
| C8 ⧫ | `benchmark-query` consumes `$MV/keymap.json` + the ingest sidecar (`ChunkBudgetGuard`) | ok — `DocumentBudget` 50 / `ChunkBudgetMultiplier` 5 (`BenchmarkQueryScenario.cs:40-41`); 19,967 / 5,183 clears the guard |
| C9 | Task 4 consumes Task 1's `TEI_MAX_BATCH_TOKENS` | ok — step 1's `grep -c 'TEI_MAX_BATCH_TOKENS' Iverson.Server/docker-compose.yml` asserts the merged edit before any TEI start |
| C10 ⧫ | Task 5 step 4 consumes the bge-base snapshot set; Task 4 step 1 is the guard that it exists | ok — both endpoints now name `scifact-bge-base-qdrant-snapshots/` (CIR-2 §2.2/§2.3's fix), the directory holds the two files the guard counts, and the restore loop's name derivation parses both of them |
| C11 ⧫ | Task 4 step 5 → Task 5 step 3(a)/step 5: the run identity (`composite`) written by `benchmark-query` and read back for the gate document | → §2.1 — the artifact carries the field; the command that reads it back cannot match it |
| C12 ⧫ | Task 5 step 4's schema re-registration against the just-restored collections | ok — `IntelligenceCollectionManager.ApplyCollectionAsync` creates only when the collection is absent, throws on a dimension mismatch, migrates only a missing named vector, and otherwise logs "up to date"; both models are 768 and the live object collection carries both `body_vector` and `body_centroid`, so the restored snapshot is not rebuilt |

### Rule-like content (both failure directions)

| Row | Rule | Disposition |
|---|---|---|
| R1 | `group_rows` orders by `int(chunk_index)` | ok — the payload stores it as a string live; under-order (string sort) is pinned by a test, over-order impossible (one row per chunk point) |
| R2 | `resolve_parents`: `parent_id` → `key_to_ulong` → object id; missing parents reported, not skipped | ok — both directions pinned by tests; the live key→id derivation reproduced exactly |
| R3 | `collapse_by_doc`: max per doc, descending, truncate AFTER | ok — matches `DocumentRanking.CollapseByDocId` in all three respects including the first-seen tie rule (LINQ `OrderByDescending` and Python `sorted` are both stable over insertion order) |
| R4 | `rank_chunk_hits` exits on an unresolved parent | ok — under-inclusion (silent skip) pinned by a test; over-inclusion impossible, the map is the object collection's full key set |
| R5 | `wait_for_index` eligibility (`green` AND `indexed > 0`) | ok on its cited evidence; the residual is D5 |
| R6 | Under-fill check `< DOCUMENT_BUDGET` on either arm, written before exiting | ok both directions — the live 250-hit search yields 204 distinct parents, so the guard does not fire spuriously on this corpus, and the multivector arm always fills 50 from 5,183 points |
| R7 | `written != expected_rows` reconciliation against `points_count` | ok — `points_count` equals `POST /points/count {"exact": true}` on the live chunks collection (19,967 both ways), so the two sides of the comparison are like for like |
| R8 | Compose interpolation at each `up` | ok — `EMBED_MODEL_ID` is referenced only by `tei-embed` (`:177`) and `BENCH_EMBED_MODEL` only by `iverson-api` (`:444`) and `iverson-worker` (`:529`), so each single-service `up` sets exactly the variable that service reads and the unset final `up` resolves both to the bge-base defaults |
| R9 | Snapshot-name parsing `${f%%-6802952876034638*}` | ok — both bge-base snapshots carry that peer id and parse to the two collection names |
| R10 | `ls -d …/scifact-gte-20*/ \| tail -1` as the identity rule for "Task 4's run directory" | ok both directions — over-inclusion is fixed (the snapshot directory no longer matches), and under-inclusion (an empty match) leaves `MV` empty, which the `\|\| exit 1` guard turns into a stop rather than a silent wrong path |
| R11 | Grep patterns as extraction rules across Tasks 4–5 | one over-strict pattern → §2.1. Every other pattern was executed against its real producer this round and matched: Qdrant collection info and snapshot-create (compact JSON), TEI `/info` (compact), the API `/build` endpoint (compact), `docker inspect`'s Env array, `docker exec … du -sh` (`130M\t/qdrant/…`) |

### Dropped candidates (failed the literal-wrongness test, or already in the review history)

| Row | Candidate | Why dropped |
|---|---|---|
| D1 | Task 3 step 5 still says "The box is on the SciFact nomic baseline (Global Constraints)", while Global Constraints now says bge-base — the round-2 fix corrected the constraint and left the citation pointing at a section that contradicts it | Already in the review history: CIR-2 §0 row **T3-smoke** identified the same false statement and dispositioned it `ok despite its stale rationale`. The evidence has not changed and the commands are unaffected — `nomic-embed-text:latest` is still resident in Ollama (re-verified live) and is 768-dim like the live chunk vectors, so the smoke exercises the mechanics it claims to. Re-raising would duplicate a prior round |
| D2 | Task 4 step 2's `up` still carries `--profile tei`, and Global Constraints still justifies "never call `stack.py`" with an out-of-tier stop that no longer happens | Both are in the history: CIR-2 §0 **D2** (the `--profile tei` no-ops) and **D1** (the `stack.py` rationale). Re-confirmed inert this round — `docker compose --profile tei config --services` on 2.40.3 accepts the undefined profile and prints the identical service list — and the directive `stack.py` justifies is still correct |
| D3 | Global Constraints' "Model id everywhere … the API recreate (`BENCH_EMBED_MODEL` and `EMBED_MODEL_ID`)" overstates: `EMBED_MODEL_ID` is read only by `tei-embed` | Verified by grep over `docker-compose.yml` — the variable appears at `:167-168` (comment) and `:177` only. Setting it on an `iverson-api`-only `up` is inert, not harmful; no command's outcome changes |
| D4 | Task 5 step 3(a) drops the `--baseline` the spec's §6 step 7 first bullet carries, splitting one invocation into (a) structural + (b) gated | The gated numbers are identical; only the Holm family size differs (2 rather than 3), and the family is the plan's own choice of comparison set, not a spec-stated outcome |
| D5 | `wait_for_index`'s `indexed_vectors_count > 0` clause rests on arithmetic about *storage size*; nothing in the spec's §10 probes or either prior round measured what `GET /collections/{name}` reports for a **multivector** collection specifically, and a zero there would abort `build` after the 11 h ingest | The same eligibility question — could `wait_for_index` refuse a correctly-built collection — is CIR-2 §0 row **R5** and span item **(f)**, dispositioned `ruled out`; the cited evidence (live segment sizes, `indexing_threshold` 10,000 KB) is unchanged, re-derived this round, and still supports it. Verifying the residual would require creating a multivector collection, which this round's read-only mandate forbids. Recorded here rather than re-raised. Note the consequence is bounded: `build`'s output is `tee`'d, `cmd_query` requires only `status == "green"`, so a spurious refusal is visible and the arm is recoverable |
| D6 | Task 5 steps 3 and 4 use `$MV` but only step 1 exports it, so a per-step shell loses it | In the history: CIR-1 §2.3 raised exactly "Task 5 uses `$MV` in steps 1 and 3 but never sets it", and the applied fix scoped the export to step 1. Same issue, unchanged evidence |
| D7 | Task 4 step 2's `/info` expectation lists its four values in a different order than the JSON emits them | CIR-1 §0 **D2**. The check is a value set read by a human; all four print |
| D8 | `python3 multivector.py build … \| tee` masks `build`'s exit status, so `query` runs on the next line regardless | CIR-1 §0 **D1** |
| D9 | Task 5 step 4 `docker start iverson-starrocks` immediately before recreating the API, while StarRocks takes minutes to become healthy | Speculation about a timing window with no demonstrated failure: the API's StarRocks cold-start gate and circuit breaker exist for this, `restart: unless-stopped` retries, and the `until curl -sf /build` loop waits. No concrete failure path |
| D10 | Task 4 step 5 recreates only `iverson-api`, while spec §6 step 4 names `iverson-api iverson-worker` | The worker is deliberately stopped by Task 4 step 1 for box quiet and restored by Task 5 step 4; it plays no part in `benchmark-query`. A defensible deviation, not a broken outcome |

## 1. Verified-plan-assumptions cross-check

| # | Status | Fresh-read evidence |
|---|---|---|
| P1 | still holds | `ls Iverson.Server/Iverson.LoadTest/scripts/` on `main` at `65c0b25`: `enrich_bench.py freshstack_to_jsonl.py ingest-contract.json ingest.py report.py sample_corpus.py stack.py test_report.py` — neither file present |
| P2/P27 | still holds | `awk NR>=173&&NR<=187`: `:173 tei-embed:`, `:174 image:`, `:175 container_name: iverson-tei-embed`, `:176 restart: unless-stopped`, `:177` byte-exactly the quoted command; no `profiles:` key anywhere in the service; `docker compose config` renders the three-item list with `BAAI/bge-base-en-v1.5` interpolated; `config --services \| grep -c '^tei-embed$'` → 1 |
| P3 | still holds | `import ingest` under a socket guard: no connect; all named symbols exported; defaults `benchmark_documents_tenant_bypass` / `benchmark_documents_chunks_tenant_bypass`; `HTTP_TIMEOUT_SECONDS` 300, `EMBED_RETRIES` 2, `QDRANT_URL` `http://localhost:6333` |
| P4 | still holds | `test_report.py:14-15` — `sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))` then `import report  # noqa: E402` |
| P5 | still holds | `inspect.signature`: `key_to_ulong(key: str) -> int`, `embed(text, model, document_prefix, embed_url)`; `ingest.py:533-535` posts `{model, input}` to `f"{embed_url}/v1/embeddings"`, `:557` returns `data[0]["embedding"]`; section header `:531` |
| P6 | still holds | `TrecRunWriter.cs:7` documents the six-column format, `:31` emits `{queryId} Q0 {docId} {rank} {score:F6} {runTag}`; `BenchmarkQueryScenario.cs:287-288` passes `flags.ConfigLabel` as the run tag |
| P7 | still holds | `report.py:117-153` (`*.trec` glob on a directory, qrels excluded by abspath, files and directories mixed); `:542-548` excludes the baseline by abspath |
| P8 | still holds | `print_stats` reads `documents/chunks/embed_calls/embeds_saved/elapsed_seconds`; an on-disk sidecar (`scifact-bge-base-2026-09-04/keymap.json.stats.json`) carries all five |
| P9 | still holds | live scroll with `with_payload: ["key","docId"]` returned exactly those two keys plus `next_page_offset` alongside `points` |
| P10 | still holds | `GET /collections/benchmark_documents_multivector_tenant_bypass` → `Not found: Collection … doesn't exist!`; the collection list is the four benchmark/vector_docs collections plus `iverson-probe` |
| P12 | still holds | `docker exec iverson-qdrant du -sh …` → `130M\t/qdrant/storage/collections/benchmark_documents_chunks_tenant_bypass` |
| P13 | still holds | `BenchmarkQueryScenario.cs:220` (`.meta.json`), `:284-288` (both `.trec`); Phase 1 plan `:808-812`, `:858-862`; `/home/ben/iverson-benchmark-data/bench-env.sh` exists |
| P14 | still holds | every cited Phase 1 line re-read at its stated number: run-dir copy `:819`, TEI recreate `:821-823`, ingest `:826`, snapshot loop `:839-846`, schema delete + API recreate + identity checks `:851-857`, restore `:905-921`. The Phase 2 citation the rewritten Task 5 step 4 adds (`:516-525`) also resolves, inside that plan's Task 4 (which starts at `:479`) |
| P15 | still holds | `docker compose config \| grep '^name:'` → `name: iversonserver` |
| P17 | still holds | by construction; no later-task symbol is used earlier, and Task 3's insertions name functions Task 2's block defines |
| P18 | still holds | `python3 -m pytest --version` → `pytest 9.1.1` |
| P19 | still holds | `scifact-run-2026-08-26/` holds `beir/`, `qrels.trec` and `runs/`; the copy commands are Phase 1 `:819` verbatim |
| P20 | still holds | `grep -rln 'auto-truncate\|tei-embed'` over `*.cs *.py *.json *.sh *.yml *.yaml` (excluding worktrees) → seven files; a targeted `grep -n 'auto-truncate'` over the six non-compose hits returns only `charts/tei/templates/statefulset.yaml:49,51`, the chart's own args. Nothing asserts the compose `command` text |
| P22 | still holds | re-ran `ir_measures.read_trec_run` on the plan's sample line → `ScoredDoc(query_id='1', doc_id='15830352', score=0.812345)` |
| P23 | still holds | `.gitignore:49` is `**/docs/plans/`; `git check-ignore -v docs/plans/2026-09-GATE-multivector.md` reports that rule; `git ls-files docs/plans \| wc -l` → 49; the file does not exist |
| P24 | still holds | `git log --oneline -8`: lowercase imperative subjects, no Conventional-Commits type |
| P26 | still holds | live info carries `status/points_count/indexed_vectors_count/segments_count` and `config.params.vectors.body_vector.size`; live named search returns `result[].{id,score,payload}`; live scroll returns `result.points[]` + `result.next_page_offset` |
| P28 | still holds | `POST /points/count {"exact": true}` on the chunks collection → 19,967, equal to its `points_count` |
| P29 | still holds | live `/info` on `iverson-tei-embed` emits `"max_batch_tokens":16384` |
| P30 | still holds | project renders as `iversonserver`; the plan now runs from `main` itself, the same directory basename |
| P31 | still holds | `docs/plans/` holds both `2026-09-GATE-embedding-migration.md` and `2026-09-GATE-reranker-phase1.md` |
| P32 | still holds | live: `tei-embed` `/info` → `BAAI/bge-base-en-v1.5`; `_iverson_schema` holds a `BenchmarkDocument` row; `scifact-bge-base-qdrant-snapshots/` holds 2 `.snapshot` files + `RESTORE.md`; collections at 19,967 / 5,183, 768 dims |
| P33 | still holds | `docker-compose.yml:437, 444` (api) and `:528, 529` (worker); `depends_on tei-embed: condition: service_healthy` at `:500-501` and `:569-570` |
| P34 | still holds | `Program.cs:403-415` wraps `EnsureInitializedAsync()` in try/catch and logs a warning; `SchemaRegistrationOrchestrator.cs:136` calls `EnsureInitializedAsync(ct)` inside registration |
| P35 | still holds | `iverson-worker:` at `:510`, `restart: unless-stopped` inside that block |
| P36 | still holds | the claim is about the *old* glob and remains true; the plan no longer uses it (see R10) |
| P37 | still holds on its cited evidence | `indexing_threshold` 10,000 KB (live `optimizer_config`); the chunks collection's 61 MB over 2 segments is HNSW-indexed (16,678 / 19,967), so a single-storage collection of that size clears the threshold. The assumption as scoped is about storage size, not about the counter's semantics under a multivector layout — see §0 **D5** |
| P38 | still holds | `GET /api/tags` lists `nomic-embed-text:latest` alongside `qwen2.5:3b` and five other embedding models |
| P39 | still holds | the P14 re-check above |

**Span check** — plan dependencies no listed or inherited assumption covers:

- (a) **The API starts, serves `/build`, and registers schemas with `iverson-starrocks`, `iverson-kafka`, `iverson-zookeeper`, `iverson-jaeger` and `iverson-worker` stopped** — Task 4 step 1 stops all five and Task 4 step 5 then recreates only `iverson-api` and runs `benchmark-query` against it. No P-row covers it. **Verified by precedent:** the Phase 1 plan's Task 6 step 1 carries a byte-identical stop list (`:783`), and its step 6 (`:851-862`) and Task 7 step 5 (`:905-919`) run the same `up -d --no-deps iverson-api` → `until curl -sf 127.0.0.1:8081/build` → `benchmark-query --help` → `benchmark-query` sequence to completion; the artefacts that sequence produced are on disk (`scifact-bge-base-2026-09-04/runs/bge-base.*`). Holds.
- (b) **`benchmark-query --help` reaches schema registration and prints `Schemas registered.` before failing on the missing corpus path** — the idiom appears three times in the plan. Verified: `Program.cs:18` takes `args[0]` as the command and parses `args[1..]` as flags, so `--help` is a flag rather than the command; `needsTenantAndSchema` (`:88`) includes `benchmark-query`; `Console.WriteLine("Schemas registered.\n")` is `:164`; only afterwards does `BenchmarkQueryScenario.RunAsync` (`:61-64`) throw on the empty `--corpus-path`. The `grep -q … && echo SCHEMA-OK` therefore prints. Holds.
- (c) **Re-registering the `BenchmarkDocument` schema after a snapshot restore does not drop or rebuild the restored collections** — Task 5 step 4 restores from snapshot and then re-registers. Verified: `IntelligenceCollectionManager.ApplyCollectionAsync` (`:46-110`) creates only when the collection is absent, throws on a dimension mismatch, migrates only when a declared named vector is missing, and otherwise logs "up to date"; bge-base and gte are both 768, and the live object collection (written by `ingest.py`) already carries both `body_vector` and `body_centroid`. Holds.
- (d) **`EMBED_MODEL_ID` is consumed only by `tei-embed`** — Task 4 step 5 passes it on an `iverson-api`-only `up`. Verified by grep: the variable appears at `:167-168` (comment) and `:177` only. Inert, so the step is correct as written. Holds.
- (e) **The `composite` field is extractable from `benchmark-query`'s sidecar with the pattern Task 4 step 5 uses** — → §2.1.
- (f) **`docker compose --profile tei` is accepted when no such profile is defined** — Task 4 step 2 still passes it. Re-verified read-only on compose 2.40.3: `--profile tei config --services` returns the identical list, exit 0. Holds.

## 2. Literal-wrongness findings

1. **Task 4 step 5's composite check greps for `"composite":"…"` (no space), but `benchmark-query` writes its sidecar indented, so the key is `"composite": "…"` — the command prints nothing and the plan's run-identity check silently produces no evidence.** The line is

   ```bash
   grep -o '"composite":"[^"]*"' $MV/runs/gte-chunks-api.meta.json                 # equals step 1's composite
   ```

   `BenchmarkQueryScenario` serialises the sidecar with `SidecarWriteOptions = new() { WriteIndented = true }` (`Iverson.Server/Iverson.LoadTest/Scenarios/BenchmarkQueryScenario.cs:54-55`, applied at `:220-222`), so `System.Text.Json` emits `"composite": "7d3a15092f963723"` with a space after the colon. Run against the real artefact this round:

   ```
   $ grep -o '"composite":"[^"]*"'  …/scifact-bge-base-2026-09-04/runs/bge-base.meta.json   # exit 1, no output
   $ grep -o '"composite": "[^"]*"' …/scifact-bge-base-2026-09-04/runs/bge-base.meta.json
   "composite": "7d3a15092f963723"
   ```

   The same pattern at Task 4 step 1 (`curl -s 127.0.0.1:8081/build | grep -o '"composite":"[^"]*"'`) is correct — the HTTP endpoint returns compact JSON (`{"composite":"faf832574f7b14b5",…}`, confirmed live), which is exactly why the defect is easy to miss: the pattern works at the producer and fails at the consumer. The Phase 1 plan this block was copied from uses the spaced pattern for the sidecar (`docs/plans/2026-09-04-embedding-migration-implementation-plan.md:862`, `grep -o '"composite": "[^"]*"'`); the space was dropped in transcription. Consequence: the last check of Task 4 — the one Global Constraints calls load-bearing ("record it: every run this plan produces must carry it") and that Task 5 step 3(a) and step 5 both depend on ("`gte-chunks-api` carries Task 4's composite") — produces empty output after an ≈ 11 h ingest, leaving a worker to either stop on a phantom failure or record the identity match with no evidence for it. **Fix:** use `grep -o '"composite": "[^"]*"' $MV/runs/gte-chunks-api.meta.json` (the Phase 1 form), or make it whitespace-tolerant: `grep -o '"composite":[[:space:]]*"[^"]*"'`.

2. **Task 4 step 1's abort path directs the worker to "Task 5 step 6's loop" to restore the baseline; Task 5 step 6 is a two-line `git add -f` / `git commit`, and the restore loop is Task 5 step 4.** The sentence closing step 1 reads "Any other point count or size: restore the bge-base baseline first (Task 5 step 6's loop) and stop." Task 5 step 6 is

   ```bash
   git add -f docs/plans/2026-09-GATE-multivector.md
   git commit -m "record the multivector experiment gate verdict"
   ```

   — no loop, and at Task 4 step 1 time that file does not exist, so a worker following the pointer literally gets `fatal: pathspec 'docs/plans/2026-09-GATE-multivector.md' did not match any files` and no restore. The procedure the sentence means is Task 5 step 4 (delete the three collections, upload from `scifact-bge-base-qdrant-snapshots/`, delete the schema row, recreate `tei-embed` on bge-base, restart the stopped containers). The mis-citation survived round 2's fix — the fix corrected "nomic" to "bge-base" in the same sentence and left the step number — and it is the only instruction the plan gives for recovering an off-baseline box before Task 4 step 3's `ingest.py --drop` destroys what is there. Without it, the plan's own "restore … first" precondition has no executable referent. **Fix:** change "Task 5 step 6's loop" to "Task 5 step 4's restore loop".

## 3. Forced decisions

No forced decisions found.

## 4. Previously addressed

- **CIR-2 §2.1** (Task 1's edit target was `:175`, now `container_name:`, and step 2's third check asserted a profile gate Phase 2 removed) — resolved: the **Files** header, the File-Structure entry and step 1 all name `:177`, which is byte-exactly the quoted command line; the third check now reads `# 1 — default service, no profile (Phase 2)` and prints `1` live; the two inert `--profile tei` flags were dropped from the render commands.
- **CIR-2 §2.2** (Global Constraints asserted a nomic baseline, and Task 4 step 1's snapshot guard counted the nomic snapshot set) — resolved: the precondition now names the bge-base baseline with `tei-embed` serving `BAAI/bge-base-en-v1.5` and API/worker on defaults, step 1 adds a live `/info` model-id check, and the snapshot count is repointed at `scifact-bge-base-qdrant-snapshots/` (verified: 2 `.snapshot` + `RESTORE.md`).
- **CIR-2 §2.3** (Task 5 step 4 stopped `tei-embed`, restored the nomic snapshots, waited on an unemittable log line, and never restarted the worker) — resolved: the block now restores from `scifact-bge-base-qdrant-snapshots/`, force-recreates `tei-embed` on bge-base and waits on `/info`, `docker start`s the four other containers Task 4 step 1 stopped, brings up `iverson-api` **and** `iverson-worker` with `BENCH_EMBED_MODEL`/`TEI_MAX_BATCH_TOKENS` unset, greps both logs for `model=BAAI/bge-base-en-v1.5`, and only then runs the `SCHEMA-OK` check — which now has a live embedding backend behind it.
- **CIR-2 §2.4** (`$MV` recovery resolved to `scifact-gte-qdrant-snapshots`) — resolved: the glob is narrowed to `scifact-gte-20*/`, the trailing slash is stripped, and the sidecar guard is `||`-chained so a wrong or empty value stops the step.
- **CIR-2 §2.5** (the model observation was computed against `rerank-a0` rather than the bge-base M1 run) — resolved: `$BGE` is exported, invocation (c) takes `--baseline $BGE/runs/bge-base.chunks.trec` and adds the four `.similar`/`rerank-a0` runs spec §6 step 7 enumerates, the expected-output note names both mismatch sources, and Task 5 step 5's arms table lists five arms with the observation stated as `gte-chunks-api` vs `bge-base`.
- **CIR-2 §1 P2/P27** (the assumption cited `:175` and a `--profile tei` render) — resolved: the row now cites `:177`, records `:175`/`:176`, and states that the service has had no `profiles:` key since `8db18dd`.
- **CIR-2 §1 P31** (the gate-document template was recorded as not yet existing) — resolved: the row now records that `2026-09-GATE-embedding-migration.md` exists; both named templates are present under `docs/plans/`.
- **CIR-2 span (a)–(h)** — all eight were promoted into the assumptions table as P32–P39 and each was re-verified this round.

## 5. Recommendation

⚠️ **Approve with literal-wrongness fixes** — §1 has no failed assumptions and the span check found no uncovered dependency that could not be verified in-round; §2 has two findings, both one-line text fixes in Task 4 (an over-strict grep pattern that makes the run-identity check silently empty, and a cross-reference that points the baseline-restore instruction at a git commit); §3 is empty. All five of round 2's findings and both of its failed assumptions are confirmed resolved, and the Python in Tasks 2–3 re-verified clean against `main` and the live stack. Address §2 before SDD, then proceed via `update-implementation-plan` or manual edits.
