# Critical Implementation Review: 2026-09-04-multivector-experiment-implementation-plan (Round 2)

**Plan:** `/home/ben/repositories/Iverson/docs/plans/2026-09-04-multivector-experiment-implementation-plan.md`
**Verified plan-level assumptions section:** present

⚠️ 51 commits since plan-write time (SHA `5563454`); cited file:line references re-checked under §1.

The drift is not cosmetic. At plan-write time the plan's runtime did not exist on `main`: it was verified against the `embedding-migration` worktree at `f2706a4`. Since then embedding-migration **Phase 1** (`c0d08dd`) and **Phase 2** (`8db18dd`) merged, and the source spec was revised three times for that merge (`8c137d0`, then CDR-2 `32cc020` and CDR-3 `d678dbe`). Three facts the plan is built on inverted: `tei-embed` is now a **default** compose service (no `profiles:`, `restart: unless-stopped`) that **`iverson-api` and `iverson-worker` both `depends_on … service_healthy` and embed through**; the box's baseline is **bge-base**, not nomic, and nomic is deployed nowhere; and the spec's model-observation baseline moved from `rerank-a0` to the bge-base M1 run.

Evidence base for this round: `main` at `d678dbe` read directly (no worktree); the live stack read but never written — `docker ps`, `docker compose config`, `docker compose --dry-run up`, Qdrant collection info / payload-only scrolls, TEI `/info`, one `psql -tAc` select; `ingest.py` imported under a socket guard; `report.py` and `ir_measures` exercised read-only; `~/repositories/iverson-benchmark-corpora/` listed. No container, collection, table or repository file was written except this review.

## 0. Coverage enumeration

### Tasks × surfaces

| Row | Surface | Disposition |
|---|---|---|
| T1-target | Task 1 **Files** header + step 1 "Replace line 175" | → §2.1 — `docker-compose.yml:175` is `container_name: iverson-tei-embed`; the `command:` line is `:177` |
| T1-text | Task 1 step 1 before/after text | ok — the "before" string is byte-exact at `:177`; the "after" line keeps `${EMBED_MODEL_ID:-BAAI/bge-base-en-v1.5}` and appends `"--max-batch-tokens", "${TEI_MAX_BATCH_TOKENS:-16384}"`, which is spec §3.2's default-preserving shape |
| T1-verify-1 | Task 1 step 2 first pipeline (`-A 9` … `grep -A 5 'command:'`) | ok — re-rendered this round: without `profiles:` the rendered service now emits `command:` as its FIRST key, so `-A 9` still spans all six command lines; injecting the two new items into the live render and re-running the pipeline printed the full five-item list ending `- --max-batch-tokens` / `- "16384"` |
| T1-verify-2 | Task 1 step 2 second pipeline (`TEI_MAX_BATCH_TOKENS=4096 … grep -c`) | ok — same simulation with `"4096"` substituted printed `1`, as annotated |
| T1-verify-3 | Task 1 step 2 third command (`config --services \| grep -c '^tei-embed$'   # 0 — still profile-gated`) | → §2.1 — ran live: prints **1** |
| T1-profile | `--profile tei` on both step-2 renders | ok — an undefined profile name is accepted by compose 2.40 and changes nothing; harmless, not a failure |
| T1-commit | Task 1 step 3 | ok — tracked file; subject matches the repo's lowercase-imperative `area:`-prefixed convention (`git log --oneline -8`) |
| T2-tests | Task 2 step 1 test file (10 tests) | ok — every asserted value re-derived: `ingest.key_to_ulong("29beccc1-3d1c-577b-a9a2-114337e70200")` → `817174487868073` live; `float` formatting, collapse and percentile expectations unchanged since round 1 and the module they test is unchanged |
| T2-red | Task 2 step 2 expected `ModuleNotFoundError` | ok — `multivector.py` still absent from `scripts/` on `main` (P1) |
| T2-code | Task 2 step 3 `multivector.py` (pure fns, Qdrant helpers, `build`, `stats`, CLI) | ok — every `ingest` symbol it names exists on `main` and was imported under a socket guard this round: `qdrant_request(method, path, body=None)` `:279`, `key_to_ulong(key:str)->int` `:229`, `embed(text, model, document_prefix, embed_url)` `:533`, `DEFAULT_OBJECT_COLLECTION`/`DEFAULT_CHUNKS_COLLECTION` → `benchmark_documents_tenant_bypass` / `…_chunks_tenant_bypass`, `HTTP_TIMEOUT_SECONDS` 300, `EMBED_RETRIES` 2, `QDRANT_URL` `http://localhost:6333`. `chunks_info["config"]["params"]["vectors"]["body_vector"]["size"]` resolves live (768). Qdrant is still 1.18.2 (`GET /`) |
| T2-index | `wait_for_index` (`green` **and** `indexed_vectors_count > 0`, else exit) | ok — the live *object* collection is green with `indexed_vectors_count 0`, which looks like a counterexample but is not: it carries **two** named vector storages (`body_vector`, `body_centroid`), so 31.8 MB over 2 segments is ~7.9 MB per (segment, vector) — under `indexing_threshold` 10,000 KB. The multivector target holds one unnamed storage of the same 61 MB the chunks collection holds (19,967 × 768 × 4), and that collection *is* indexed live (16,678 / 19,967, `segments_count` 2), so the target clears the threshold and the guard will not spuriously exit |
| T2-green | Task 2 step 4 "10 passed" | ok — pytest 9.1.1 on Python 3.14.4 (P18) |
| T2-smoke | Task 2 step 5 smoke against scratch collections | ok — reads the live chunks/object collections (now bge-base, still 768/19,967/5,183, payloads unchanged), writes only `scratch_multivector_smoke` and `/tmp/mv-smoke`, both cleaned up; the api-key literal in the `curl DELETE` matches `docker-compose.yml`'s `QDRANT__SERVICE__API_KEY` |
| T2-commit | Task 2 step 6 | ok |
| T3-tests | Task 3 step 1 (two `rank_chunk_hits` tests) | ok — assertions match the function as written; `SystemExit` on the unknown parent |
| T3-red | Task 3 step 2 expected `AttributeError` | ok |
| T3-code | Task 3 step 3 `rank_chunk_hits` + `cmd_query` + subparser, at the three named insertion points | ok — search body `{vector:{name:"body_vector",vector}, limit:250, with_payload:["parent_id"]}` and query body `{query:[vec], limit:50, with_payload:["docId"]}` are spec §10 rows 4–5; `try/finally write_outputs()` still writes partial evidence on every exit path; the green-status precondition and `index_state` sidecar are CIR-1 §2.2's fix and are present |
| T3-embed | `ingest.embed(text, args.model, "", args.embed_url)` for queries | ok — signature verified live; `ingest.document_prefix_for("Alibaba-NLP/gte-modernbert-base")` → `''` (gte is absent from both prefix tables; `EmbeddingPrefixes.cs` Table holds only nomic, arctic, bge-base, bge-small) |
| T3-green | Task 3 step 4 "12 passed" | ok |
| T3-smoke | Task 3 step 5 smoke (nomic via Ollama on 11434) | ok despite its stale rationale — the step says "the box is on the SciFact nomic baseline", which is false now, but the command still works: `nomic-embed-text:latest` is still in the Ollama volume (`/api/tags` this round) and both nomic and bge-base are 768-dim, so the scratch collection accepts the query vector and the smoke checks mechanics, not relevance |
| T3-commit | Task 3 step 6 | ok |
| T4-pre-guard | Task 4 step 1 snapshot-set precondition (`ls scifact-512-qdrant-snapshots/*.snapshot \| wc -l   # 2`) | → §2.2 |
| T4-pre-counts | Task 4 step 1 point-count / vector-size / composite / health checks | ok — live values match (19,967 & 768, 5,183); the six-container health loop omits `iverson-tei-embed`, which step 2 recreates anyway; the `--dry-run … \| grep -o "Container iverson-[a-z-]* *Recreate"` pipeline is informational and well-formed |
| T4-stop | Task 4 step 1 `docker stop iverson-worker iverson-starrocks iverson-kafka iverson-zookeeper iverson-jaeger` | ok as a stop (an explicit `docker stop` defeats `restart: unless-stopped`), but nothing restarts them → the worker half is → §2.3 |
| T4-tei | Task 4 step 2 run dir + `TEI_MAX_BATCH_TOKENS=4096 EMBED_MODEL_ID=… --profile tei up -d --no-deps --force-recreate tei-embed` + `/info` check | ok — `--profile tei` is now inert but harmless; `/info` exposes `max_batch_tokens` (live: `16384` on the bge-base container), so the four-value grep works (P29) |
| T4-ingest | Task 4 step 3 background ingest + completion checks | ok — every flag exists (`ingest.py --help`); `--drop` still touches only `args.object_collection`/`args.chunks_collection` (`:791-793`); sidecar is `<key-map-path>.stats.json` (`:772`) with `documents/chunks/model/embed_url/chunk_max_chars/chunk_step/elapsed_seconds`; `verify_contract(args.model)` (`:766`) is the permissive call, so gte falls through to the `__default__` golden and does not abort the run |
| T4-snap | Task 4 step 4 gte snapshot loop + `RESTORE.md` | ok — byte-identical to Phase 1 plan `:839-846`, which still sits at those lines; two `%s`, two args; the emitted `RESTORE.md` points at `../scifact-512-qdrant-snapshots/RESTORE.md` for the loop, which is where the loop text lives |
| T4-api | Task 4 step 5 schema delete + gte API recreate + identity check + `benchmark-query` | ok — `Embeddings__ModelId=${BENCH_EMBED_MODEL:-BAAI/bge-base-en-v1.5}` (`docker-compose.yml:444`) and `Embeddings__BaseUrl=http://tei-embed:80` (`:437`); `--no-deps iverson-api` was dry-run this round and touches **only** `iverson-api`, so spec §3.2's second hazard (a dependent recreate dragging `tei-embed` along) does not fire; expected log `documentPrefix= queryPrefix=` is right for gte; `benchmark-query` writes `<label>.{chunks,similar}.trec` + `.meta.json` (`BenchmarkQueryScenario.cs:220, 287-288`) |
| T4-shell | Task 4 shell state (`K`, `SCI`, `D`, `MV`) | ok — all set in-task before use |
| T5-mv | Task 5 step 1 `export MV=$(ls -d …/scifact-gte-* \| tail -1)` | → §2.4 |
| T5-run | Task 5 step 1 build → query → stats | ok in mechanics (the index-readiness ordering CIR-1 raised is fixed by `wait_for_index` + `cmd_query`'s green precondition); blocked only by the `$MV` value → §2.4 |
| T5-snap | Task 5 step 2 multivector snapshot | ok — same loop; the produced name carries the `-6802952876034638-` peer id every snapshot in every directory on this box carries |
| T5-score-a | Task 5 step 3 (a) `--run $MV/runs --stats-path …` | ok — `resolve_run_paths` still globs `*.trec` from a directory and excludes the `--qrels` path by abspath (`report.py:117-153`); `--stats-path` still reads `documents/chunks/embed_calls/embeds_saved/elapsed_seconds` (`:308-340`) |
| T5-score-b | (b) two `--run` files + `--baseline gte-chunks-raw.chunks.trec` | ok — `run_paired_statistics` excludes the baseline by abspath (`:542-551`) and reads the baseline run once into a list before iterating measures (`:559`), so the Holm family is 2 and the CI line prints per measure |
| T5-score-c | (c) model observation vs `rerank-a0` | → §2.5 |
| T5-restore | Task 5 step 4 restore block | → §2.3 |
| T5-gate | Task 5 step 5 gate-document prose (arms table, box-state closing line) | → §2.5 (four-arm table, `rerank-a0` as the observation) and → §2.3 (closing "nomic restored … TEI stopped") |
| T5-commit | Task 5 step 6 `git add -f` | ok — `.gitignore:49` `**/docs/plans/`; `git check-ignore -v` confirms; 49 files tracked under `docs/plans/` (P23 said 45) |
| GC | Global Constraints block | → §2.2 (the nomic-baseline precondition). The `--profile tei` reference and the `stack.py` rationale ("its out-of-tier stop halts `iverson-tei-embed`") are both stale — `stack.py`'s TIERS now include `tei-embed` (`stack.py:84-87`) so it would recreate it on bge-base rather than stop it — but the *directive* ("never call `stack.py`") is unchanged and still correct, so nothing executable breaks; `--pair` never appears anywhere in the plan |

### Cross-task interface contracts (⧫ = persistence boundary)

| Row | Contract | Disposition |
|---|---|---|
| C1 | Task 3 consumes Task 2's `scroll`, `require_collection`, `collapse_by_doc`, `trec_lines`, `summarize_latency`, `add_common_args` | ok — all defined in Task 2's block with the arities Task 3 calls |
| C2 ⧫ | `build` reads chunk payload `{parent_id, chunk_index}` + `vector.body_vector` and object payload `{key, docId}` written by `ingest.py` | ok — live payload-only scrolls this round returned exactly `{"parent_id":"323fbe8d-…","chunk_index":"3"}` and `{"key":"d2b2694a-…","docId":"21993510"}` from the *bge-base* collections, so the shape survived the migration |
| C3 ⧫ | Task 3 `query` reads the multivector payload `docId` written by Task 2 `build` | ok — `resolve_parents` writes `{key, docId, chunk_count}`; the query asks `with_payload: ["docId"]` |
| C4 ⧫ | Task 5 consumes Task 3's two `.trec` files and `raw-latency.json` | ok — names are the module constants + `.chunks.trec`; sidecar carries `chunks`/`multivector` summaries plus `index_state` |
| C5 ⧫ | Task 5 consumes Task 4's `$MV` run dir, labels and `keymap.json.stats.json` | **the artifact contract is fine; the parameter sourcing is not** → §2.4 (this is the eval-side call whose parameter comes from a directory glob rather than from Task 4's shell) |
| C6 ⧫ | `report.py` / `ir_measures` consume the raw TREC files | ok — re-ran `read_trec_run` on the plan's sample line this round → `ScoredDoc(query_id='1', doc_id='15830352', score=0.812345)` |
| C7 ⧫ | `benchmark-query` consumes `$MV/keymap.json` + the ingest sidecar (`ChunkBudgetGuard`) | ok — `DocumentBudget` 50 / `ChunkBudgetMultiplier` 5 at `BenchmarkQueryScenario.cs:40-41`; 19,967/5,183 clears the guard |
| C8 | Task 4 consumes Task 1's `TEI_MAX_BATCH_TOKENS` | ok — step 1's `grep -c 'TEI_MAX_BATCH_TOKENS'` asserts the merge before any TEI start |
| C9 ⧫ | Task 5 step 3(c) consumes an *existing* run as the model-observation baseline | → §2.5 — `$SCI/runs/rerank-a0.chunks.trec` exists, but the spec's baseline is now `scifact-bge-base-2026-09-04/runs/bge-base.chunks.trec` (verified present, composite `7d3a15092f963723`) |
| C10 ⧫ | Task 5 step 4 consumes a snapshot set; Task 4 step 1 is the guard that it exists | → §2.2 / §2.3 — the guard names `scifact-512-qdrant-snapshots/` (nomic) and the restore consumes it; the artifact the *current* box state needs is `scifact-bge-base-qdrant-snapshots/` (verified: 2 `.snapshot` + `RESTORE.md`) |
| C11 | The gate doc consumes `storage.json` + `raw-latency.json` + the ingest sidecar + `.meta.json` | ok — every number step 5 asks for has a named producer |

### Rule-like content (both failure directions)

| Row | Rule | Disposition |
|---|---|---|
| R1 | `group_rows` orders by `int(chunk_index)` | ok — payload stores it as a string live (`"chunk_index":"3"`); under-order (string sort) is pinned by a test, over-order impossible (one row per chunk point) |
| R2 | `resolve_parents`: `parent_id` → `key_to_ulong` → object id; missing reported, not skipped | ok — both directions pinned by tests; the live key→id derivation reproduced exactly |
| R3 | `collapse_by_doc`: max per doc, descending, truncate AFTER | ok — keep-first, truncate-before and ascending are each pinned |
| R4 | `rank_chunk_hits` exits on an unresolved parent | ok — under-inclusion (silent skip) pinned by a test; over-inclusion cannot occur, the map is the object collection's full key set |
| R5 | `wait_for_index` eligibility: `green` AND `indexed > 0` | ok both directions — over-inclusion (accepting a yellow/0 collection) is what the check exists to prevent; under-inclusion (refusing a correctly-built collection) is ruled out by T2-index's segment arithmetic. Every producer of a Qdrant collection `status` was enumerated: `green`/`yellow`/`grey`/`red` — `grey` and `red` both fall to the deadline branch and exit non-zero, which is the intended outcome |
| R6 | Under-fill check `< DOCUMENT_BUDGET` on either arm, written before exiting | ok |
| R7 | `written != expected_rows` reconciliation against `points_count` | ok — `points_count` is exact (P28); both arms of the comparison come from the same live shapes |
| R8 | Compose interpolation at each `up` (`EMBED_MODEL_ID`, `BENCH_EMBED_MODEL`, `TEI_MAX_BATCH_TOKENS`) | ok for Tasks 1 and 4 — but the *final* `up` in Task 5 step 4 relies on the defaults resolving to nomic/Ollama, which they no longer do → §2.3 |
| R9 | Restore-loop name parsing `${f%%-6802952876034638*}` | ok — every snapshot in both candidate directories carries that peer id |
| R10 | `ls -d …/scifact-gte-* \| tail -1` as an identity rule for "Task 4's run directory" | → §2.4 — over-inclusive: it also matches the snapshot directory the plan itself creates |

### Dropped candidates (failed the literal-wrongness test)

| Row | Candidate | Why dropped |
|---|---|---|
| D1 | The plan's `stack.py` rationale is now wrong (it would recreate `tei-embed` on bge-base, not stop it) | The directive it justifies ("never call `stack.py`") is unchanged and still correct; no command in the plan calls it. A stale explanation with a correct instruction breaks nothing at execution time |
| D2 | `--profile tei` appears on four commands and is now a no-op | Compose accepts an undefined profile silently; every affected command was re-rendered or dry-run this round and behaves identically |
| D3 | Task 4 step 1's health loop omits `iverson-tei-embed`, now a hard dependency of the API | Step 2 force-recreates it two commands later and waits on `/info`; an unhealthy TEI at step 1 changes nothing |
| D4 | `iverson-prometheus` and `iverson-authentik-worker` are running and not in Task 4 step 1's stop list, against "nothing else runs on the box" | Both are near-idle sidecars; the constraint's purpose is to keep the 11 h ingest off contended CPU, and neither contends. Not a mechanic the plan got wrong |
| D5 | P3/P5/P6/P7/P8/P13's cited line numbers have all shifted by a few lines | Every cited symbol was re-read at its new line and every assumption's *claim* holds; a line-number drift that resolves to the same code is not an execution failure |
| D6 | The chunks collection is green with only 16,678 / 19,967 vectors HNSW-indexed, so the per-chunk arm is partly exact search | That is Qdrant's default behaviour for this collection and exactly what the API arm runs under — spec §3.1 gates on "default HNSW on both", not on full indexing |
| D7 | `report.py` will print `BUILD UNKNOWN` banners for the two raw runs that the plan does not mention | Informational; the gate reads the CI line, which prints regardless |

## 1. Verified-plan-assumptions cross-check

| # | Status | Fresh-read evidence |
|---|---|---|
| P1 | still holds | `ls Iverson.Server/Iverson.LoadTest/scripts/` on `main` — no `multivector.py` / `test_multivector.py` |
| **P2/P27** | **FAILED** | `docker-compose.yml:175` is `container_name: iverson-tei-embed`; `:177` is the `command:` line. The service has **no `profiles:` key** since Phase 2 (`8db18dd`) and carries `restart: unless-stopped` (`:176`). `docker compose config --services \| grep -c '^tei-embed$'` → **1**. The `--profile tei config` render still interpolates the default correctly (re-run this round), so only the line pointer and the profile-gating claim failed → §2.1 |
| P3 | still holds | `import ingest` under a socket guard: no connect; all named symbols exported; defaults `benchmark_documents_tenant_bypass` / `…_chunks_tenant_bypass`; `HTTP_TIMEOUT_SECONDS` 300, `EMBED_RETRIES` 2 |
| P4 | still holds | `test_report.py` `sys.path.insert` + `import report  # noqa: E402` |
| P5 | still holds | `key_to_ulong(key: str) -> int` now `:229`; `embed(text, model, document_prefix, embed_url)` now `:533`; posts `{model, input}` to `<embed_url>/v1/embeddings`, returns `data[0]["embedding"]` |
| P6 | still holds | `TrecRunWriter.cs` writes `{queryId} Q0 {docId} {rank} {score:F6} {runTag}`, rank from 1; `BenchmarkQueryScenario.cs:287-288` passes `ConfigLabel` as the run tag |
| P7 | still holds | `report.py:117-153` (dir → sorted `*.trec`, qrels excluded by abspath, files and dirs mixed); `:542-551` baseline excluded by abspath |
| P8 | still holds | `report.py:308-340` reads `documents, chunks, embed_calls, embeds_saved, elapsed_seconds` |
| P9 | still holds | live scroll with a `with_payload` field list returned exactly those keys plus `next_page_offset` |
| P10 | still holds | `benchmark_documents_multivector_tenant_bypass` is absent from `GET /collections` |
| P12 | still holds | `du -sh` inside `iverson-qdrant` prints `<size>\t<path>` |
| P13 | still holds | `BenchmarkQueryScenario.cs:220` (`.meta.json`), `:287-288` (both `.trec`); Phase 1 plan `:808-812, 858-862` |
| P14 | still holds | every cited Phase 1 plan line re-read at its stated number: run-dir copy `:819`, TEI recreate `:821-823`, ingest `:826`, snapshot loop `:839-846`, schema delete + API recreate + identity checks `:851-857`, restore `:905-921`. (The spec's revised `:837` citation is the *step header* of the same step — not a conflict.) **The restore block's content is now wrong for this box, but the citation is accurate** → §2.3 |
| P15 | still holds | `docker compose config` → `name: iversonserver` |
| P17 | still holds | by construction; no later-task symbol is used earlier |
| P18 | still holds | `pytest 9.1.1`, Python 3.14.4 |
| P19 | still holds | `scifact-run-2026-08-26/{beir/queries.jsonl (300 rows, `_id`+`text`), qrels.trec (300 distinct qids), runs/}` |
| P20 | still holds | `grep -rln 'auto-truncate\|tei-embed'` over `*.cs *.py *.json *.sh *.yml` on `main` now hits seven files (`stack.py`, `ingest.py`, `Iverson.Launcher/Program.cs`, the Helm `tei` statefulset and the two Embeddings test files, plus compose). Every hit was read: they reference the *service name*, a URL, or the chart's own args — **nothing asserts the compose `command` text**, so the edit remains consumer-safe |
| P22 | still holds | re-ran `ir_measures.read_trec_run` on the plan's sample line |
| P23 | still holds | `.gitignore:49` `**/docs/plans/`; `git check-ignore -v` confirms; 49 tracked files under it (was 45 at plan-write, 46 at round 1) |
| P24 | still holds | `git log --oneline -8` |
| P26 | still holds | live info carries `status/points_count/indexed_vectors_count/segments_count/config.params.vectors.body_vector.size`; live scroll carries `result.points[]` + `next_page_offset` |
| P28 | still holds | `points_count` is the exact count on both live collections |
| P29 | still holds | live `/info` on `iverson-tei-embed` includes `"max_batch_tokens":16384` |
| P30 | still holds | `docker compose config` → project `iversonserver`; the running containers carry that label. The plan now runs from `main` itself, which is the same directory basename, so the point is moot but true |
| **P31** | **FAILED (evidence changed; benign)** | `docs/plans/2026-09-GATE-embedding-migration.md` **now exists** (Phase 1 Task 8 shipped it). The assumption's stated fact is false; its conclusion — that a template is available for Task 5 step 5 — is now satisfied by the *primary* named template rather than the fallback. No action needed |

**Span check** — plan dependencies no listed or inherited assumption covers:

- (a) **The live box is on the bge-base baseline, not nomic.** Verified live: `tei-embed` `/info` → `BAAI/bge-base-en-v1.5`; `_iverson_schema` holds a `BenchmarkDocument` row from 2026-09-05; `scifact-bge-base-qdrant-snapshots/` exists. No P-row covers the baseline's model identity, and Global Constraints asserts the opposite → §2.2, §2.3.
- (b) **`iverson-api` and `iverson-worker` both embed through `tei-embed` and default to bge-base.** Verified: `docker-compose.yml:437, 444` (api) and `:528, 529` (worker), both `depends_on tei-embed: service_healthy` (`:500-501, :569-570`). No P-row covers it → §2.3.
- (c) **The API's startup embedding init is non-fatal, but schema registration is not.** Verified: `Program.cs:402-414` catches and warns; `SchemaRegistrationOrchestrator.cs:136` calls `EnsureInitializedAsync` and `SchemaBuilder.cs:67` reads `embedding.Dimension`. This is what makes Task 5 step 4's final `SCHEMA-OK` check the thing that fails → §2.3.
- (d) **`iverson-worker` exists as a compose service that Task 4 step 1 stops.** Verified `docker-compose.yml:510-516` (`restart: unless-stopped`, so an explicit `docker stop` holds). Nothing in the plan restarts it → §2.3.
- (e) **`ls -d …/scifact-gte-*` also matches the snapshot directory the plan creates.** Verified by reproducing the glob on a scratch pair under this shell's `LANG=C.UTF-8`: `scifact-gte-qdrant-snapshots` sorts last → §2.4.
- (f) **The multivector collection will clear Qdrant's `indexing_threshold`, so `wait_for_index` will not spuriously refuse a correctly-built collection.** Verified by segment arithmetic against the two live collections (see §0 T2-index): holds.
- (g) **`nomic-embed-text` is still resident in the Ollama volume**, which Task 3 step 5's smoke needs. Verified: `GET /api/tags` lists `nomic-embed-text:latest`. (Note `ollama-init` now pulls only `qwen2.5:3b` — the model survives because the volume is named, not because anything re-pulls it.) Holds today.
- (h) **The Phase 1 plan's cited line numbers still resolve.** Verified (P14). Holds.

## 2. Literal-wrongness findings

1. **Task 1 points at `docker-compose.yml:175`, which is now `container_name: iverson-tei-embed`, and its third verification command asserts a profile gate that no longer exists.** The plan's **Files** header ("Modify: `Iverson.Server/docker-compose.yml:175`"), its File-Structure entry ("`docker-compose.yml:175` — `tei-embed` command gains …") and step 1's instruction ("Replace line 175") all name a line that, on `main` at `d678dbe`, reads `    container_name: iverson-tei-embed`; the `command:` line the step quotes is at `:177`. A worker who replaces line 175 by number deletes the container name, at which point `docker exec iverson-tei-embed`, `docker stats --no-stream … iverson-tei-embed` (Task 4 step 2) and `stack.py`'s `CONTAINER` map all break against a compose-generated name. Separately, step 2's third check — `docker compose config --services | grep -c '^tei-embed$'   # 0 — still profile-gated` — was run live this round and prints **1**: Phase 2 removed `profiles:` from the service (`docker-compose.yml:173-187`), which is exactly what spec §3.2 now records. The step's own annotation therefore reports a failure on a correct edit. (Step 2's first two pipelines are fine: re-rendering the live config with the two new items injected showed `-A 9` still spans the whole command block, because without `profiles:` compose emits `command:` as the service's first key.) **Fix:** change all three pointers to `:177`; drop the third command or replace it with the Phase 2 plan's own check — `docker compose config --services | grep -c '^tei-embed$'   # 1 — default service, no profile` — and drop the now-inert `--profile tei` from the two render commands.

2. **Task 4 step 1's snapshot precondition guards the wrong artifact, and Global Constraints states a precondition that is false.** Global Constraints says "the box is on the SciFact nomic baseline (5,183 object / 19,967 chunk points, 768 dims, API defaults)"; spec §6's preconditions now read "the box is on the SciFact **bge-base** baseline … `tei-embed` serving bge-base, API and worker on defaults". Live confirmation: TEI `/info` → `BAAI/bge-base-en-v1.5`. The counts are identical between the two baselines, so none of step 1's checks distinguishes them — the only check that could is `ls …/scifact-512-qdrant-snapshots/*.snapshot | wc -l   # 2`, and that names the **nomic** set. Task 4 step 3 then runs `ingest.py --drop`, which deletes both live collections; the only copy of the state being destroyed is `scifact-bge-base-qdrant-snapshots/` (verified: 2 `.snapshot` files + `RESTORE.md`), and the plan never checks that it exists. The guard passes while proving nothing about the artifact the run actually depends on. **Fix:** change the Global Constraints precondition to the bge-base baseline (`tei-embed` serving `BAAI/bge-base-en-v1.5`, API and worker on defaults), add `curl -s http://127.0.0.1:8091/info | grep -o '"model_id":"[^"]*"'   # BAAI/bge-base-en-v1.5` to step 1, and repoint the snapshot check at `ls /home/ben/repositories/iverson-benchmark-corpora/scifact-bge-base-qdrant-snapshots/*.snapshot | wc -l   # 2`.

3. **Task 5 step 4 restores the wrong baseline, waits on a log line that can no longer be emitted, stops the embedding backend the API and worker now require — so its own closing check cannot pass — and never restarts the worker.** The block is Phase 1 plan Task 7 step 5 verbatim (`:905-921`), which predates both the Phase 2 merge and the spec's revision. Four concrete failures, in execution order:
   - `docker compose --profile tei stop tei-embed` leaves the backend down. Since Phase 2 the API and worker embed through it (`Embeddings__BaseUrl=http://tei-embed:80`, `docker-compose.yml:437, 528`) and `depends_on` it (`:500-501, :569-570`). Spec §6 step 8 replaced the stop with a **recreate on bge-base** for exactly this reason.
   - `for f in *.snapshot` runs in `scifact-512-qdrant-snapshots/` — the **nomic** collections. The box is restored to nomic vectors while `iverson-api` comes back configured for `${BENCH_EMBED_MODEL:-BAAI/bge-base-en-v1.5}` (`:444`). Dimensions match (768 both), so nothing errors: the next SciFact benchmark on this box silently scores nomic vectors with bge-base queries.
   - `docker logs iverson-api 2>&1 | grep "EmbeddingService initialized"   # model=nomic-embed-text dimension=768` cannot match. The compose default is bge-base and nomic is deployed nowhere; with TEI stopped the API takes `Program.cs`'s catch branch and logs `Embedding service not initialized at startup` instead. The pipeline is not `&&`-chained, so this failure is silent and execution continues.
   - `dotnet run … benchmark-query --help | grep -q "Schemas registered." && echo SCHEMA-OK` then **cannot** print. Schema registration calls `EnsureInitializedAsync` (`SchemaRegistrationOrchestrator.cs:136`) and reads `embedding.Dimension` (`SchemaBuilder.cs:67`), which needs a live `/v1/embeddings` — and `tei-embed` was stopped two commands earlier. The task ends leaving the box with no embedding backend, no registered `BenchmarkDocument` schema, and `iverson-worker` still stopped from Task 4 step 1 (`restart: unless-stopped` does not undo an explicit `docker stop`; spec §6 step 8 names api **and** worker).

   **Fix:** replace the block with spec §6 step 8's procedure — restore from `scifact-bge-base-qdrant-snapshots/`, delete the gte `_iverson_schema` row, then `EMBED_MODEL_ID=BAAI/bge-base-en-v1.5 docker compose up -d --no-deps --force-recreate tei-embed` (waiting on `/info` for `BAAI/bge-base-en-v1.5`) and `docker compose up -d --no-deps iverson-api iverson-worker` from a shell with neither `BENCH_EMBED_MODEL` nor `TEI_MAX_BATCH_TOKENS` set; expect `model=BAAI/bge-base-en-v1.5 dimension=768` in **both** logs before the `SCHEMA-OK` check; restart the other containers Task 4 step 1 stopped; and change Task 5 step 5's closing box-state line from "nomic restored … TEI stopped" to the bge-base state.

4. **Task 5 step 1's `$MV` recovery resolves to the snapshot directory, not the run directory.** The line is `export MV=$(ls -d /home/ben/repositories/iverson-benchmark-corpora/scifact-gte-* | tail -1)`. Task 4 step 4 creates `scifact-gte-qdrant-snapshots/` in that same parent, and it matches the same glob. Reproduced this round on a scratch pair under this shell's `LANG=C.UTF-8`: `ls -d …/scifact-gte-*` emits `scifact-gte-2026-09-06` then `scifact-gte-qdrant-snapshots`, so `tail -1` yields **the snapshot directory** — deterministically, on every run, because a date-prefixed name always sorts before `q`. The guard on the next line (`ls $MV/runs/gte-chunks-api.meta.json`) errors but is not `&&`-chained, so execution continues: `tee $MV/multivector-build.log` writes into the snapshot directory, `multivector.py query --run-dir $MV` raises `FileNotFoundError` on `scifact-gte-qdrant-snapshots/beir/queries.jsonl`, `stats --run-dir $MV` writes `runs/storage.json` there, and step 3's `--run $MV/runs` exits with `--run …: not a file or directory`. **Fix:** narrow the glob to the dated form and make the guard blocking, e.g. `export MV=$(ls -d /home/ben/repositories/iverson-benchmark-corpora/scifact-gte-20*/ | tail -1); MV=${MV%/}; ls $MV/runs/gte-chunks-api.meta.json || exit 1`.

5. **The model observation is computed against `rerank-a0`, but the spec's stated outcome is `gte-chunks-api` vs the bge-base M1 run.** Spec §2's decision table (row "Model observation baseline (2026-09-06)"), §3.1's arms table, §6 step 7's second bullet and §7's closing paragraph all now name `scifact-bge-base-2026-09-04/runs/bge-base.chunks.trec` as the baseline, with `rerank-a0` demoted to context and required to appear as a **fifth** row in the score table (§6 step 9: "score table (five runs, both RPC files)"). The plan's Task 5 step 3(c) runs `--run $MV/runs/gte-chunks-api.chunks.trec --qrels $MV/qrels.trec --baseline $SCI/runs/rerank-a0.chunks.trec`, and Task 5 step 5's arms table lists four arms (`rerank-a0`, `gte-chunks-api`, `gte-chunks-raw`, `gte-multivector-raw`) with no bge-base row. Executed as written, the gate document would report "is gte an upgrade on nomic?" — a question the migration already retired — and would not contain the observation §7 defines. The baseline run is on disk and needs no re-run (verified: `bge-base.chunks.trec`, `bge-base.similar.trec`, `.meta.json` composite `7d3a15092f963723`). **Fix:** repoint (c) at the bge-base run as `--baseline` and add the `rerank-a0` and `.similar` rows the spec's §6 step 7 bullet enumerates (the three run dirs' `qrels.trec` are byte-identical, so one `--qrels` scores them all); add `bge-base` to Task 5 step 5's arms table and change the observation sentence to name it.

## 3. Forced decisions

No forced decisions found.

## 4. Previously addressed

- **CIR-1 §2.1** (Task 1 step 2's `grep -A 6` pipelines could not show what the step claimed) — resolved: the plan now uses `grep -A 9 '^  tei-embed:' | grep -A 5 'command:'`, and re-rendering the current config with the edit injected confirms both pipelines produce the annotated output. (The *third* command in that same step is a separate, newer defect — §2.1.)
- **CIR-1 §2.2** (`query` measured latency before Qdrant had built the HNSW index, and `stats` recorded a state that did not hold during measurement) — resolved: Task 2's `multivector.py` now carries `wait_for_index` with a 600 s bound and a `green`-with-zero-indexed refusal, `cmd_query` requires `status == "green"` on both collections before the loop and writes each collection's `{status, points_count, indexed_vectors_count, segments_count}` into `raw-latency.json` as `index_state`, and Task 5 step 1's expected output names the indexed count.
- **CIR-1 §2.3** (`$MV` used but never set in Task 5) — addressed in intent: Task 5 step 1 now opens with an `export MV=…` and a `K=…`. The recovery expression it chose is itself wrong — see §2.4.

## 5. Recommendation

⚠️ **Approve with literal-wrongness fixes** — §1 has two failed assumptions (P2/P27, materially; P31, benignly); §2 has five findings, four of them consequences of the embedding-migration merge that landed after the plan was written; §3 is empty. The plan's Python (Tasks 2–3) is unaffected by the drift and re-verified clean; the damage is concentrated in Task 1's edit target, Task 4's preconditions, and Task 5's shell state, restore and scoring. Address §2 before SDD, then proceed via `update-implementation-plan` or manual edits.
