# Critical Implementation Review: 2026-09-08-at-rest-encryption-node-disks-and-allowlist-implementation-plan (Round 2)

**Plan:** `/home/ben/repositories/Iverson/docs/plans/2026-09-08-at-rest-encryption-node-disks-and-allowlist-implementation-plan.md`
**Verified plan-level assumptions section:** present

⚠️ 14 commits since plan-write time (SHA `51234e6`); cited file:line references re-checked under §1. Two are this plan's own (`f6fa41d` writes it, `6ae73d2` applies round 1's fixes) and one is round 1's review (`305a093`); the remaining eleven are the `similar-via-chunks` C# work. `git diff --stat 51234e6..HEAD -- Iverson.Server/deploy docs/runbooks .github .gitlab-ci.yml` is empty, so no file this plan cites has moved.

The §0 sweep was built against the current plan text before `-critical-review-1.md` and `6ae73d2` were read in detail. Round 1's two findings and the paragraphs they touched are three ordinary rows below (T4c, T4e, R5, R6), not the search area.

All empirical claims below were re-derived by applying every one of the plan's edits to a scratch copy of `Iverson.Server/deploy/terraform`, both CI files and the runbook under the session scratchpad. The repository was not modified (`git status --porcelain` empty at exit).

## 0. Coverage enumeration

### Tasks × surfaces

| # | Surface | Disposition |
|---|---|---|
| T0a | Header — Goal / Architecture / Tech stack | ok — `terraform v1.9.8`, `helm v3.16.4`, `kubeconform v0.8.0` locally; CI pins Terraform `1.7.5` (`.github/workflows/deploy-validate.yml:118`, `.gitlab-ci.yml:67`) and Helm `3.15.0` (`:23` / `:12`). No `.terraform.lock.hcl` is tracked anywhere under `Iverson.Server/deploy/terraform` (`find` returns nothing), so CI resolves the same floating-within-pin versions this review resolved: `google 5.45.2`, `azurerm 3.117.1`, `kubernetes 2.38.0`, `helm 2.17.0` — all inside the plan's stated pins |
| T0b | Global Constraints — provider pins, creation-time-only, no hand-copied list, fmt-is-a-write-step, commit-message style | ok — `kubernetes ~> 2.31` / `helm ~> 2.14` at `modules/operators/main.tf:3-4`; the creation-time claim is greenfield-guarded and no cluster exists; the no-hand-copy rule is realised by the seven resource references in Task 3 Step 3, matching `modules/operators/outputs.tf:3-9`; fmt claim is T0c |
| T0c | Global Constraint — "CI runs `fmt -check` without `-recursive`, which never reaches the module files" | ok — re-derived empirically rather than from documentation: injecting a deliberately mis-aligned block into `modules/operators/main.tf` leaves `terraform -chdir=<scratch>/aws fmt -check` at exit 0 while `terraform fmt -check -recursive <scratch>` flags the file at exit 3 |
| T0d | File Structure — Create / Modify / Test lists | ok — all three create paths are new (`ls -d modules/operators/charts` → no such file), all six modify paths exist and are tracked, and the "no tests created" claim matches the tree: no test project references the deploy directory. The "four edit regions" for the runbook is exact (Task 4 has four content steps); `:7-8` is explicitly a *non*-edit site |
| T1a | Task 1 Step 1 — prose locators and the `node_config` code block | ok — `grep -n node_config modules/cluster-gcp/main.tf` returns exactly `:222` and `:261`; `google_container_node_pool.general` at `:201`, `.pools` at `:244`; both blocks carry the identical `service_account = google_service_account.gke_nodes.email` anchor. Applied to a scratch copy, `terraform fmt` produced the plan's code block character-for-character, including the widened `boot_disk_kms_key` alignment column |
| T1b | Task 1 Step 1 — "The `pools` block is identical except `machine_type = each.value.machine_type`" | ok — true of the four lines the code block shows. The `pools` `node_config` additionally carries `labels`, `taint` and `workload_metadata_config` blocks below the insertion point, but the instruction anchors on the `service_account` line, not on block equality, so the insert is unambiguous |
| T1c | Task 1 Step 2 — fmt/init/validate commands | ok — all four run as written from the repo root. `fmt -check -recursive` flagged the file before the write step and exited 0 after; `terraform -chdir=…/gcp validate` → `Success! The configuration is valid.` |
| T1d | Task 1 Step 3 — commit | ok — path is repo-root-relative and tracked; `git check-ignore` returns non-zero for `modules/cluster-gcp/main.tf` |
| T2a | Task 2 Step 1 — `disk_encryption_set_id` locator and code block | ok — `azurerm_kubernetes_cluster.this` at `:152`, `sku_tier` at `:158`; `azurerm_disk_encryption_set.data_volumes` at `:78`. After `terraform fmt` the scratch copy reproduces the plan's block exactly, and `role_based_access_control_enabled` at `:160` stays in its own alignment group because a blank line separates it |
| T2b | Task 2 Step 2 — `kubelet_disk_type` on both pool declarations | ok — `default_node_pool` at `:162` and `azurerm_kubernetes_cluster_node_pool.pools` at `:213`, both with a `vm_size` anchor; neither sets `kubelet_disk_type` today (`grep` returns nothing tree-wide). After fmt the scratch reproduces both code blocks exactly. The plan's `default_node_pool` excerpt elides `vnet_subnet_id`, which sits directly below the insertion point, but the prose anchors on `vm_size`, so the elision cannot mislead an inserting agent |
| T2c | Task 2 Step 1 — "Do not add `enable_host_encryption`" | ok — matches the spec's Scope exclusion (`…-design.md:37-40`); no pool sets it today |
| T2d | Task 2 Step 3–4 — validate and commit | ok — `terraform -chdir=…/azure validate` → `Success!` with both edits applied |
| T3a | Task 3 Interfaces — Consumes/Produces | ok — see C1–C3, C6 |
| T3b | Task 3 Step 1 — `Chart.yaml` / `values.yaml` | ok — `Chart.yaml` matches `Iverson.Server/deploy/helm/iverson/charts/prometheus/Chart.yaml` field for field (apiVersion v2, type application, version 0.1.0, appVersion "1.0.0"); `helm lint` on the created chart exits 0 with only `[INFO] Chart.yaml: icon is recommended` |
| T3c | Task 3 Step 2 — `templates/policy.yaml` | ok — every named field checked against the 1.30 schema, not just the two the plan calls out: `spec.failurePolicy`, `matchConstraints.resourceRules[].{apiGroups,apiVersions,operations,resources}`, `validations[].{expression,reason,messageExpression}`, and on the binding `policyName`, `validationActions`, `matchResources.namespaceSelector.matchLabels`. `kubeconform -kubernetes-version 1.30.0` → `Valid: 2, Invalid: 0, Errors: 0, Skipped: 0`, naming both kinds. `apiGroups: [""]` is the core group, which is where `persistentvolumeclaims` lives |
| T3d | Task 3 Step 3 — the `helm_release` block | ok — all seven `kubernetes_storage_class` addresses and `kubernetes_namespace.iverson` resolve in-module (`modules/operators/main.tf:12,139,148,157,166,175,184,193`); no existing release is named `pvc_storageclass_policy` (the five are `cloudnative_pg`, `strimzi`, `starrocks_operator`, `cluster_autoscaler`, `aws_load_balancer_controller`). Every reference is an implicit dependency, so the release is ordered after the namespace and all seven classes without a `depends_on`. `terraform fmt` left the block untouched — it is already canonical |
| T3e | Task 3 Step 4 — the two CI insertions | ok — both applied to scratch copies and parsed with PyYAML. GitHub: the new step lands as the fifth entry of `jobs.helm-lint.steps`, and its 6-space list indent correctly terminates the preceding `run: \|` block scalar. GitLab: the new line lands as the third entry of `helm-lint.script`, terminating the preceding `\|` block. Both files still parse |
| T3f | Task 3 Step 5 — lint / template / fmt / init / validate | ok — every command run as written. `helm lint` → `1 chart(s) linted, 0 chart(s) failed`; `helm template … --set 'allowedStorageClasses={iverson-postgres,iverson-prometheus}'` rendered `in ["iverson-postgres","iverson-prometheus"]` and `"; allowed: iverson-postgres, iverson-prometheus"`, and the binding's `kubernetes.io/metadata.name: iverson`; `terraform -chdir=…/aws validate` → `Success!` with the chart directory present |
| T3g | Task 3 Step 6 — multi-path commit | ok — none of the four paths is gitignored. `.gitignore:60-61` ignores `**/.terraform/` only, not a `charts/` directory under the terraform tree; `git check-ignore` on the chart path returns non-zero |
| T4a | Task 4 intro + Step 1 — coverage-paragraph rewrite, `:7-8` left standing | ok — `:12-14` are still the three lines beginning "Commands are written against AWS"; `:7-8`'s invariant ("runnable as written except the bracketed `<zone>` in check 3's GCP variant") survives, because none of the three commands Steps 2–3 add introduces a bracketed substitution. `command grep -n "aws-only"` over the runbook returns only `:13` (rewritten by Step 1) and `:189` (deleted by Step 4), so no third site is left asserting check 5 is AWS-only |
| T4b | Task 4 Step 2 — check-4 enforcement evidence and negative test | ok — anchor `## 4. No volume escaped the set` at `:136` is unique; the section ends at `:151`. The `kubectl get validatingadmissionpolicies` / `validatingadmissionpolicybindings` plurals match the 1.30 API paths. The heredoc PVC is structurally complete and its explicit `storageClassName: standard` keeps the DefaultStorageClass mutating plugin out of the path, so the request reaches validating admission with the class the test intends |
| T4c | Task 4 Step 3 — Azure variant, as amended by `6ae73d2` | ok — see R5. The amended `--query` is now a double-quoted shell word containing a single-quoted JMESPath literal; tokenised under `set --` it arrives as one argument with the inner quotes intact, and it contains no `$`, backtick or `\` to expand, so the runbook's "runnable as written" invariant holds. `az aks show -n … -g … --query nodeResourceGroup -o tsv` and `terraform output -raw cluster_name` are both real invocations against a real root output (C4) |
| T4d | Task 4 Step 3 — GCP variant | ok — `sed 's#^gce://##'` then `IFS=/ read -r project zone instance` binds the three fields of `gce://<project>/<zone>/<instance>` in order; `$project` is bound but unused, which is harmless and matches check 3's existing omission of `--project`. `--format='value(name,diskEncryptionKey.kmsKeyName)'` is the field check 3 already reads at `:132` |
| T4e | Task 4 Step 4 — closing-section repoint, as amended by `6ae73d2` | ok — see R6 |
| T4f | Task 4 Step 4 — the three surfaces the replacement text names | ok — all three exist as real resources, so the runbook does not point an auditor at a phantom: `aws_cloudwatch_log_group.vpc_flow_logs` (`modules/cluster-aws/main.tf:30`) with `aws_flow_log.this` (`:79`), and `azurerm_log_analytics_workspace.this` (`modules/cluster-azure/main.tf:139`). AKS etcd Secrets carry no resource by construction. Wording is verbatim from the spec's Scope section |
| T4g | Task 4 Step 3 — "see the design's section 4" | ok — resolves. `docs/specs/2026-09-08-at-rest-encryption-node-disks-and-allowlist-design.md:186` is `### 4. Evidence`, and it does contain the deferral the sentence attributes to it (`:199-204`). The spec is git-tracked despite `.gitignore:51`, so a repo reader can follow the pointer; the runbook already uses this un-pathed idiom at `:72` ("per Section 7's negative check") |
| T4h | Task 4 Step 5 — commit | ok — `docs/runbooks/` is not ignored (`git check-ignore` non-zero; the file is in `git ls-files`), so the plain `git add` works |
| T4i | Markdown fence balance across Task 4 | ok — the four-backtick outer fences at plan `:341`/`:373` (Step 2) and `:377`/`:415` (Step 3) each wrap their inner ```` ```bash ```` blocks and close correctly; Steps 1 and 4 use plain three-backtick fences around prose. `6ae73d2` inserted only body text inside the Step 3 fence and rewrote one non-fenced line in Step 4, so the balance is unchanged |
| T5 | "Tasks NOT in this plan" / "Known issues inherited from spec" | ok — verbatim from the spec's Scope and Known-issues sections; the stale-comment claim is real (`modules/cluster-aws/main.tf:25`) and correctly marked off-path |

### Cross-task interface contracts

| # | Contract | Disposition |
|---|---|---|
| C1 | Task 3 produces the policy/binding object names and shape; Task 4 Step 2 consumes them as the runbook's expected output | ok — re-checked field by field against the chart Task 3 creates: policy and binding both named `iverson-pvc-storageclass`; `validationActions: [Deny]`; `namespaceSelector.matchLabels."kubernetes.io/metadata.name": iverson` (from `{{ .Release.Namespace }}`, which the release fixes to `kubernetes_namespace.iverson.metadata[0].name`); `validations[0].expression` carrying the seven names. Every expectation the runbook states is present in the rendered object |
| C2 | Task 3's `helm_release` values → the CEL list literal → "the same seven classes check 2 lists" | ok — traced across all three serialisation boundaries on real output. Terraform sources `iverson-{postgres,starrocks,qdrant,kafka,ollama,tei,prometheus}` from `modules/operators/main.tf:141,150,159,168,177,186,195`; runbook check 2 (`:79-80`) lists exactly those seven strings; `toJson` renders them as a CEL list literal, confirmed by running `helm template` |
| C3 | Tasks 1–2 produce the key/set Task 4 Step 3's expectations name | ok — GCP: `boot_disk_kms_key` and the StorageClass `disk-encryption-kms-key` parameter both resolve to `google_kms_crypto_key.data_volumes.id` (`gcp/main.tf:70`). Azure: the cluster's `disk_encryption_set_id` and the StorageClass `diskEncryptionSetID` parameter both resolve to `azurerm_disk_encryption_set.data_volumes.id` (`azure/main.tf:70`). One key/set per cloud, as the amended closing paragraph asserts |
| C4 | Task 4 Step 3's first command consumes the **azure root** output `cluster_name` | ok — `azure/main.tf:75` re-exports `module.cluster.cluster_name`; `modules/cluster-azure/outputs.tf:1` sources it from `azurerm_kubernetes_cluster.this.name`, which is `var.cluster_name`, so `${CLUSTER}-rg` reproduces `azurerm_resource_group.this.name` at `modules/cluster-azure/main.tf:8`. Not sensitive, so `output -raw` works |
| C5 | Task ordering — P5's "Tasks 1–3 touch disjoint files and share no symbol" | ok — verified by applying all three tasks' edits to one scratch tree simultaneously; all three roots validate, and each task's whole-tree `terraform fmt -recursive` is idempotent over the others because the pristine tree is already format-clean |
| C6 | Task 3's `chart = "${path.module}/charts/pvc-storageclass-policy"` → the Helm provider's chart loader at apply time | ok — this crosses from Terraform's path space into the provider's filesystem access and no P item covers it. Reproduced minimally: for a module sourced as `../mod` from a root, `path.module` evaluates to `"../mod"`, i.e. relative to the `-chdir` root directory, which is also the Terraform process's working directory. Confirmed the resulting literal path loads: `cd <scratch>/aws && helm lint ../modules/operators/charts/pvc-storageclass-policy` → `0 chart(s) failed`. The `charts/` parent directory name does not make Helm treat the target as a subchart |
| C7 | Task 3's new chart directory vs. the `security-audit` job's `tfsec Iverson.Server/deploy/terraform/` | ok — the chart adds three YAML files and no `.tf` file, and tfsec's Terraform scanner only parses `.tf`/`.tf.json`, so the directory is invisible to that job. The three added Terraform attributes are hardening, not new resources, so they introduce no new rule surface |

### Rule-like content (both failure directions)

| # | Rule | Disposition |
|---|---|---|
| R1 | The CEL predicate — which PVCs are denied | ok, both directions. Under-inclusion: the rendered list is exactly the seven Terraform-created class names, and `has()` plus the list membership deny both a class-less claim (which would otherwise fall through to a cluster default) and an explicit `""`. Over-inclusion: `operations: ["CREATE"]` only, and `storageClassName` is immutable after create, so there is no UPDATE path to a disallowed class. The mutating DefaultStorageClass plugin runs before validating admission, so a class-less PVC is seen either with the cluster default (denied, correctly) or still unset (denied by `has()`) |
| R2 | The chart's degenerate render — `allowedStorageClasses: []` under default values | ok — this is the input class CI actually feeds, since both lint invocations use chart defaults. Renders `object.spec.storageClassName in []` and `"; allowed: "`, both valid YAML; `helm lint` exits 0 and `kubeconform` reports `Valid: 2, Invalid: 0` on the empty-values render too. The empty list never reaches a cluster because the `helm_release` always supplies all seven |
| R3 | Kubeconform schema conformance of the rendered pair | ok — `Valid: 2, Invalid: 0, Errors: 0, Skipped: 0`, naming both `ValidatingAdmissionPolicy` and `ValidatingAdmissionPolicyBinding`, against `-kubernetes-version 1.30.0` |
| R4 | The runbook's own coverage rule after Task 4 — is any surviving sentence falsified by the amendments? | ok — `command grep -in "aws-only\|not covered\|by design"` over the pre-amendment runbook returns `:13`, `:188` and `:189`; Step 1 rewrites the first and Step 4 deletes the other two. `:155-159` (check 5's "not the PersistentVolumes checks 1–4 cover") stays true on all three clouds once the Azure filter and the GCP boot-disk lookup are in place |
| R5 | Task 4 Step 3's `[?contains(name, 'OsDisk')]` filter, both failure directions | ok — **under-inclusion is fail-closed and the plan names the remedy in-line.** If AKS does not expose node OS disks as standalone `Microsoft.Compute/disks` resources, or names them differently, the filter returns nothing, and the amended paragraph makes an empty result a *failure* ("If this returns no rows, the check is not satisfied and must be re-derived against the agent pool's scale set … That includes the filter itself"). **Over-inclusion cannot reintroduce check 3's evidence:** `disk.csi.azure.com` names its disks after the PV (`pvc-<uuid>`), and the Azure StorageClass config at `azure/main.tf:66-71` sets no name-affecting parameter, so a CSI data disk cannot contain the substring `OsDisk`. The one proposition the filter rests on — that AKS names scale-set OS disks `…_OsDisk_…` — is exactly the proposition the spec's `### 4. Evidence` (`:199-204`) ships unverified by decision, and the amended text now says so explicitly rather than silently inheriting it. That is coherent: the spec's deferral is preserved, its trigger is reachable, and the escape hatch covers the filter as well as the enumerability question |
| R6 | Task 4 Step 4's locator, evaluated at the moment Step 4 actually runs | ok — the amended locator is content-anchored ("the ten lines from `**AKS and GKE node OS disks are not covered by this runbook, and not by design.**` to the end of the file"), and both halves are still true post-Steps-1–3. Simulated: Step 1 (−1 line, above the region), Step 2 (insert before `## 5. Node root volumes`), Step 3 (insert before `## What this evidence set does not cover`). In the resulting 232-line file the heading sits at `:221`, blank at `:222`, and the marker sentence at `:223`; `awk '/AKS and GKE node OS disks are not covered/,0' \| wc -l` returns **10**, running to EOF. Neither insertion lands below the closing heading, so the paragraph's extent, its ten-line count and its "to the end of the file" terminus are all unchanged by the three preceding steps |

## 1. Verified-plan-assumptions cross-check

Each P item re-checked by a fresh read of the cited evidence, or by re-running the cited command.

- **P1** — holds. All cited paths exist and are tracked.
- **P2** — holds. `ls -d Iverson.Server/deploy/terraform/modules/operators/charts` → `No such file or directory`; the module contains only `main.tf`, `outputs.tf`, `variables.tf`.
- **P3** — holds, exact. `modules/cluster-gcp/main.tf:73`; `modules/cluster-azure/main.tf:78`; `modules/operators/main.tf:139,148,157,166,175,184,193`; `:12`.
- **P4** — holds. The five existing release names are `cloudnative_pg` (`:21`), `strimzi` (`:32`), `starrocks_operator` (`:59`), `cluster_autoscaler` (`:77`), `aws_load_balancer_controller` (`:110`). No collision.
- **P5** — holds. Re-verified by applying all three tasks' edits to one scratch tree and validating all three roots.
- **P6** — holds. `terraform v1.9.8`, `helm v3.16.4`; `init -backend=false` succeeded on all three roots.
- **P7** — holds, exact. `.github/workflows/deploy-validate.yml:120-127` fmt/init/validate, `:28-35` the umbrella lint loop; `.gitlab-ci.yml:15-20` the same loop.
- **P8** — holds, and re-derived empirically this round rather than from the flag's documentation (§0 T0c).
- **P9** — holds. All three roots `Success! The configuration is valid.` with every planned edit applied, against `google 5.45.2` / `azurerm 3.117.1` / `helm 2.17.0` / `kubernetes 2.38.0`.
- **P10** — holds. `fmt -check -recursive` flagged `modules/cluster-azure/main.tf` and `modules/cluster-gcp/main.tf` at exit 3 before the write step, exit 0 after; `modules/operators/main.tf` was never flagged.
- **P11** — holds, exact. `1 chart(s) linted, 0 chart(s) failed` with one `[INFO] Chart.yaml: icon is recommended`; template rendered `in ["iverson-postgres","iverson-prometheus"]` and `"; allowed: iverson-postgres, iverson-prometheus"`.
- **P12** — holds, exact. `Valid: 2, Invalid: 0, Errors: 0, Skipped: 0`.
- **P13** — holds. `Iverson.Server/deploy/helm/iverson/charts/prometheus/Chart.yaml` is apiVersion v2 / type application / version 0.1.0 / appVersion "1.0.0".
- **P14** — holds. `command grep -rn "google_container_node_pool\." --include=*.tf Iverson.Server/` returns nothing (exit 1).
- **P15** — holds, exact. `modules/cluster-azure/main.tf:122` (`principal_id = azurerm_kubernetes_cluster.this.identity[0].principal_id`) and `:216` (`kubernetes_cluster_id`); `outputs.tf:1,4`. The new edge runs DES → cluster → role assignment, and nothing on the set's side references the cluster, so the graph stays acyclic.
- **P16** — holds. `modules/operators/outputs.tf` declares only `storage_class_names`.
- **P17** — holds. A non-gitignore-aware `grep -rn "at-rest-encryption-verification"` outside `docs/criticalreviews/` returns only the predecessor plan, this plan and the two specs, all by path — no code, CI or chart reference.
- **P18** — holds. `modules/cluster-azure/outputs.tf` exposes exactly `cluster_name`, `kube_config`, `node_pool_labels`, `data_volumes_des_id`; none is the node resource group.
- **P19** — holds, exact. `modules/cluster-azure/main.tf:8` is `name = "${var.cluster_name}-rg"`; `azure/main.tf:78` builds the identical string.
- **P20** — holds. Both plurals match the 1.30 API paths under `/apis/admissionregistration.k8s.io/v1/`.
- **P21** — holds. `which az gcloud aws` returns nothing (exit 1).

### Span check — plan dependencies no listed assumption covers, as scoped

1. **`path.module` resolves to a chart directory the Helm provider can actually load at apply time.** P9 covers validate-time acceptance of the release block, not the runtime path. Verified in-round (§0 C6): `path.module` for a `../modules/operators` local module evaluates to `"../mod"`-shaped relative path against the `-chdir` root, and `helm lint` on that literal path from the `aws` root directory succeeds. No finding.
2. **No `.terraform.lock.hcl` is tracked, so CI resolves providers the same way this review did.** The plan's Tech-stack line names `google ~> 5.30` and `azurerm ~> 3.90`, but the attributes were exercised against `5.45.2` and `3.117.1`. Verified in-round: `find Iverson.Server/deploy/terraform -name .terraform.lock.hcl` returns nothing, and `.gitignore` ignores only `**/.terraform/`, so CI resolves the newest version within each pin exactly as the scratch run did. No finding.
3. **The three surfaces Task 4 Step 4's replacement text names are real resources**, so the amended closing paragraph does not point an auditor at something that does not exist. No P item covers it; verified in-round (§0 T4f). No finding.
4. **The amended Azure `--query` survives shell tokenisation as a single argument with its JMESPath quotes intact.** `6ae73d2` changed the argument's outer quoting from single to double; no P item covers shell-level correctness of runbook commands, and the runbook's `:7-8` invariant depends on it. Verified in-round (§0 T4c). No finding.
5. **The design section Task 4 Step 3's new sentence cites exists and says what the sentence attributes to it.** No P item covers cross-document references introduced by the round-1 fix. Verified in-round (§0 T4g). No finding.

## 2. Literal-wrongness findings

No literal-wrongness findings.

Every §0 row carries a disposition and none reached the gate. Both round-1 fixes were re-derived rather than assumed: the Step 4 locator's "ten lines … to the end of the file" was measured against a simulated post-Steps-1–3 file and is still exactly ten lines running to EOF (R6), and the Step 3 Azure filter was checked in both failure directions — under-inclusion is fail-closed with the remedy stated in the same paragraph, over-inclusion is impossible because CSI disks are named `pvc-<uuid>` (R5). The static pass reproduced every command the plan prints; the dynamic pass covered admission-plugin ordering ahead of the policy, the apply-time chart path (C6), the release's implicit ordering behind the namespace and all seven StorageClasses (T3d), and the CI jobs' block-scalar termination (T3e).

## 3. Forced decisions

No forced decisions found.

The one candidate — that the `[?contains(name, 'OsDisk')]` filter is unverified against a real AKS node resource group — is not an unpicked either/or. The spec's `### 4. Evidence` (`:199-204`) already settled it as ship-and-verify-at-first-apply with a named re-derivation obligation, and `6ae73d2` extended that acceptance to cover the filter itself in the runbook's own text. Surfacing it as a §3 would re-open a decision the design records as taken.

## 4. Previously addressed

- **Round 1 §2.1** — Task 4 Step 3's Azure command listed the whole node resource group, so its expectation was satisfiable from the CSI data disks check 3 already reads, and its "if this returns no rows" re-derivation trigger could never fire. Resolved in `6ae73d2` by round 1's option (a): the `--query` now filters `[?contains(name, 'OsDisk')]`, and the expectation paragraph was rewritten to state that the filter is what keeps check 5 independent of check 3, with the escape hatch explicitly extended to the filter's own naming assumption. Re-checked both failure directions this round (§0 R5) — no defect introduced.
- **Round 1 §2.2** — Task 4 Step 4's `(:186-188)` locator under-scoped the ten-line closing paragraph and was stale by the time Step 4 ran, because Steps 1–3 had already changed the file's length. Resolved in `6ae73d2` by replacing it with a content anchor naming the paragraph's first line and its extent to end-of-file. Re-measured against a simulated post-Steps-1–3 file this round (§0 R6) — the description is still accurate at the moment Step 4 executes.
- **Round 1 §1** — no failed assumptions; the two wording imprecisions it recorded (P1's file count, P18's parenthetical) were left as-is, and both assumptions' substance re-confirmed above.

## 5. Recommendation

✅ **Approve as-is** — §1 has no failed assumptions and the span check found no uncovered dependency that could not be verified in-round; §2 and §3 are both empty. The plan is ready for `subagent-driven-development`. Both round-1 fixes were re-derived from evidence rather than taken on trust, and neither introduced a defect: the Step 4 locator is still exactly right at the moment it is consumed, and the Step 3 Azure filter fails closed in the only direction it can fail, with its own unverified premise named in the runbook text the step writes. Tasks 1–3 reproduced exactly as claimed — all three cloud roots validate with every planned edit applied and the chart present, `terraform fmt` reproduces the plan's code blocks character-for-character, the chart lints and renders the intended CEL list literal, the rendered pair passes `kubeconform` against Kubernetes 1.30, and both CI insertions leave their files parsing as valid YAML with the new steps in the intended jobs.
