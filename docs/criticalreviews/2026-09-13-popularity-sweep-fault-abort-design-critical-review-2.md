# Critical Design Review: 2026-09-13-popularity-sweep-fault-abort-design (Round 2)

**Spec:** `/home/ben/repositories/Iverson/docs/specs/2026-09-13-popularity-sweep-fault-abort-design.md`
**Artifact HEAD at review:** f18081d8b90f820d05d004b33c6f0e69fa3633c9
**Verified Assumptions section:** present

**Amendment anchoring.** Round 1 recorded the SHA form (`5b29301941bf90c177343e7bb6bbafa99e795ba6`). Content-identity check run on this report: `git rev-parse 5b29301:docs/specs/…md` → `257f9b79b2a0eebdc15abd22e8cf8e10c310d250`; `git hash-object` on disk → `c629e37563df3e25a1ba446445471041c21f98b6`. **Unequal — an amendment occurred.** Authoritative hunk set: `git diff 5b29301 -- <spec>` → 21 insertions, 6 deletions. Forward window `git log 5b29301..HEAD -- <spec>` → exactly one commit, `f18081d` "applied 3 fixes from 2026-09-13-popularity-sweep-fault-abort-design-critical-review-1 to …", which matches the update skill's in-band commit shape. **Family (c) — out-of-band amendment hunks — is therefore empty, stated explicitly rather than omitted.** Reverse window `git log HEAD..5b29301 -- <spec>` → empty (the anchor is still in HEAD's ancestry). `git status --porcelain -- <spec>` → empty, so this round records the SHA form.

Base-of-record: the spec still says "Verified against `41a6269`". `git diff --stat 41a6269 HEAD --` over the four source/test files this design edits is empty, so every line cite below is equally valid at the spec's stated base and at HEAD.

The §0 sweep below was built from a fresh read of the spec and the codebase **before** round 1 was read in detail; round 1 was then read in full for the never-re-raise rule, and rows whose independent derivation landed on a disposition round 1 also reached are marked as such rather than re-raised.

---

## 0. Coverage enumeration

### Sections

| # | Section | Disposition |
|---|---|---|
| S1 | Header / Status / "Fixes" line | `ok` — [existence] `git cat-file -t b32f870` and `41a6269` both resolve; `git diff --stat 41a6269 HEAD -- PopularitySignalConsumer.cs PopularitySignalReconciliationWorker.cs PopularitySignalConsumerTests.cs PopularitySignalReconciliationWorkerTests.cs` → empty output. Status line "design, not yet planned or implemented" matches: `grep -rn '\bPopularityUpdateOutcome\b' --include=*.cs .` (worktrees excluded) → zero hits, so nothing has been implemented against this spec. |
| S2 | "The problem" | `ok` — [existence] all three cites re-read at HEAD: `PopularitySignalConsumer.cs:44` is `internal async Task UpdateAsync(` (bare `Task`); the aggregate `catch` opens at `:78` and `return;`s at `:83`; `PopularitySignalReconciliationWorker.cs:74` is `catch (Exception ex)`. The "400,000 lines every ten minutes" figure is illustrative scale, not load-bearing on any design mechanic. |
| S3 | Design §1 — `UpdateAsync` reports an outcome | `ok` — [negative] the new enum's name is free: `grep -rn '\bPopularityUpdateOutcome\b' --include=*.cs .` (worktrees excluded) → **zero hits**. [existence] an `internal` enum returned from `internal` `UpdateAsync` on `internal sealed class PopularitySignalUpdater` (`:38`) compiles for the test assembly: `Iverson.Api.csproj:9-12` emits `InternalsVisibleTo("Iverson.Api.Tests")`. Convention cites → S8/A2. |
| S4 | Design §2 — exit-path mapping + the propagation contract | → R1, R2, and fix-neighborhood row **F3**. |
| S5 | Design §3 — abandon on consecutive failures | → R3, R4, R5, R6, and fix-neighborhood row **F2**. |
| S6 | Design §4 — the six tests | → R8 (test 6's totality claim) and span items SP6/SP7; tests 1–5 `ok` — [compat] each is expressible against existing fixtures: 1/2 need `_search.AggregateAsync(...).Returns(Task.FromException<EngagementAggResult?>(ex))` / `.Returns((EngagementAggResult?)null)`, both idioms present (`PopularitySignalConsumerTests.cs:149`, `:374`); 3/4 need multi-row pages, built at `PopularitySignalReconciliationWorkerTests.cs:96-100`; 3 additionally needs a recording logger → A15; 5's premise → A5. |
| S7 | "Out of scope" | `ok` — [existence] `IEngagementStoreHealthCheck.IsHealthyAsync()` is at `IEngagementStoreRoles.cs:11-15` exactly as cited (interface opens `:11`, `CheckHealthAsync` `:13`, `IsHealthyAsync` `:14`, closes `:15`). "Changing any existing log line" holds against the amended text: the diff `5b29301..HEAD` touches the abort's *new* line only — `grep -c` of the four existing log calls (`PopularitySignalConsumer.cs:80,91,115,137`) is unchanged, and the spec adds no edit to any of them. |
| S8 | "Verified assumptions" (A1–A19 + two unnumbered) | → §1. |

### Rules and operands

| # | Rule | Disposition (both directions) |
|---|---|---|
| R1 | Exit → outcome mapping (§2 table, five rows) | **over (the table claims an exit the method doesn't have):** `ok` — [totality] I enumerated every terminal point of `UpdateAsync` (`PopularitySignalConsumer.cs:44-141`) by reading the whole method: `return;` at `:83`, `return;` at `:94`, fall-off-the-end at `:141`, and propagation. Four classes plus a residual closed by construction — no sixth class is constructible. **under (an exit the table omits):** `ok` — [totality] the two filtered catches (`when (ex is not OperationCanceledException)` at `:78` and `:113`) put OCE in the residual row 5 rather than rows 1/3; row 5 is defined as "any other exception", so the partition stays complete. (Round 1's R1 reached the same partition; its dropped sub-item on row 3's "Updated" label being a branch rather than an exit is **not re-raised**.) |
| R2 | "The fifth path must keep propagating … Do not add a catch-all" — the **amended** guardrail claim | **over (claims a guardrail that doesn't exist):** `ok` — [totality] the amended text now asserts the *opposite* of round 1's retracted claim: "neither test asserts it, so a catch-all would pass the suite unchanged". I re-derived this independently by reading both test bodies in full: `PopularitySignalConsumerTests.cs:412-430` contains exactly one assertion (`await act.Should().NotThrowAsync(...)`, `:428-429`); `PopularitySignalReconciliationWorkerTests.cs:161-187` contains exactly two (`NotThrowAsync` `:181-182`, `Received(1)` on article-2 `:184-186`). A swallowing `catch { log; return Failed; }` satisfies all three. The amended claim is true. **under (the contract is real but the class does not actually propagate today):** `ok` — [totality] it does propagate: `PopularitySignalConsumer.cs:134` is `catch (RpcException ex) when (ex.StatusCode == StatusCode.NotFound)` — the only catch around the Qdrant write — and `IntelligenceVectorService.SetPayloadAsync` (`:88-99`) contains no `try`, no catch, no retry (read in full). |
| R3 | Failure-counting rule: `Failed` increments; the worker's `catch` at `:74` increments the same counter | **over (counts something that is not a fault):** `ok` — [totality] the increment set is exactly {`Failed`, propagating exception}. `Failed` is produced only by the aggregate `catch` at `:78-84`; the worker's `catch` at `:74` is unfiltered and receives only the residual class. `Skipped`/`Updated` reset (R4). Null-tenant rows `continue` at `:67` before `UpdateAsync` is called, so they produce no outcome and neither increment nor reset — the correct neutral treatment. **under (a real fault escapes the counter):** `ok` — [totality] producer sweep over `IEngagementStoreSearchService.AggregateAsync` — `grep -rn 'IEngagementStoreSearchService' --include=*.cs .` yields **two** implementations: `EngagementRepository` (`ServiceCollectionExtensions.cs:33`) and `DisabledEngagementStoreSearchService` (`:35`). Under a StarRocks outage the former's connection/query failure is not `IsExpectedMissingResourceError` (`EngagementRepository.cs:142-148` matches only `MySqlException` ParseError + "cannot find role"/"is not granted to", or code 5502 + "Unknown table"), so it propagates to `:78` → `Failed` → counted. The latter throws `EngagementStoreDisabledException` unconditionally (`DisabledEngagementStoreSearchService.cs:22-27`) → also `:78` → `Failed` → counted. Neither producer can silently bypass the counter. |
| R4 | Reset rule: "any other outcome resets it to zero", counter spans the paging loop | **over (resets when it should not, masking a fault):** `ok` — [totality] the resetting members are `Updated` and `Skipped` only. Under the targeted fault every row yields `Failed` (R3-under), so no reset can interleave. **under (fails to reset / spurious abort):** `ok` — [existence] `FetchKeysAndTenantsPagedAsync` (`PopularitySignalReconciliationWorker.cs:60-62`) sits outside the per-row `try`, so a page fetch neither increments nor resets; `MaxConsecutiveFailures = 5` < `PageSize = 500` (`:27`), so under a total fault the abort always fires inside page 1 and `afterKey` (`:82`) is never reached. (Round 1's R4 reached the same disposition independently.) |
| R5 | Threshold comparison `++consecutiveFailures >= MaxConsecutiveFailures`, and the log's three operands | **over (fires early / logs a wrong count):** `ok` — [totality] pre-increment then `>=` fires on the 5th failure and binds `{Count}` = 5, matching §3's prose ("six log lines … five queries"). The format string has exactly three placeholders (`{Signal}`, `{Count}`, `{Minutes}`) and the call passes exactly three arguments in that order. **under (fires late / never):** `ok` — [existence] the same increment-and-test statement is the single idiom §3 specifies for both increment sources. (Round 1 dropped the "implementer transcribes it into only one place" variant to critical-implementation-review; **not re-raised**.) |
| R6 | Scope rule: per-signal, not per-sweep | **over (one signal's abort suppresses more than it should):** `ok` — [existence] the abort's `return` exits only the current `SweepSignalAsync`; `SweepLoopAsync:36-37` is `foreach (var signal in options.Value.Signals) await SweepSignalAsync(...)`, and `await Task.Delay(SweepInterval, ct)` at `:39` is outside that loop, so neither the remaining signals nor the next sweep are affected — which is exactly what "Recovery is automatic" asserts. **under (the retained cost exceeds the stated bound "5 × signals"):** `ok` — [existence] `PopularitySignalValidator.ValidateAtStartup` is the startup gate (`PopularitySignalOptionsTests.cs:54-125` exercises its four checks, including duplicate/unregistered parent types), so `Signals` cannot contain an entry that is swept twice per cycle. |
| R7 | The **amended** fault-domain claim: "The counter therefore spans two fault domains — a `Failed` outcome (engagement store) and a propagating exception (Qdrant, collection resolution, api-key minting). It cannot distinguish them, so the summary line must not name a store." | **over (names a domain that cannot actually produce a propagating fault):** `dropped` — [totality] two of the three named propagating producers are total functions and cannot throw: `IntelligenceTenantScope.ResolveCollectionName` (read in full — string interpolation over `Sanitize`/`Fingerprint`, no throwing operation, and a null `CollectionName!` interpolates to `""` rather than raising), and `IntelligenceStoreConsumer.KeyToUlong` (`:705-715` — `Guid.TryParse` else `FnvHash`, total over any non-null string). Only Qdrant (`SetPayloadAsync`, no catch/retry) and `MintScopedApiKey` (`:127`, inside the NotFound-filtered try; `SymmetricSecurityKey`/`JwtSecurityTokenHandler` can throw on a short signing key) are live producers. The over-inclusion is in explanatory prose whose operative conclusion — "must not name a store" — is established by Qdrant alone and is unchanged. No asked-for behavior differs. Fails literal-wrongness. **under (a propagating domain the sentence omits):** `dropped` — [absence] the only omitted candidate is `OperationCanceledException` (excluded by both catch filters at `:78`/`:113`, caught by the worker's bare `catch` at `:74`), and it has **no producer on this path**: `UpdateAsync`'s signature (`:44-46`) takes no `CancellationToken`, `IEngagementStoreSearchService.AggregateAsync` takes none, and `IVectorWriteService.SetPayloadAsync` takes none — so no awaited call here can observe cancellation. A class with no producer cannot be under-enumerated. |
| R8 | Test 6's totality claim: "this is the only assertion in the suite that a catch-all converting the fifth path into an outcome would fail" | **over (another existing assertion already fails under a catch-all, making test 6 redundant):** `ok` — [totality] I enumerated the whole population, not a sample. `grep -rn '\bPopularitySignalUpdater\b' --include=*.cs .` (worktrees excluded) → the type is constructed or named in exactly **two** test files (`PopularitySignalConsumerTests.cs:61-62`, `PopularitySignalReconciliationWorkerTests.cs:72-73,174`); no integration or reflection-based test reaches it (`ObjectSearchVectorIntegrationTests.cs`'s "popularity" hits are the vector *ranking* signal, a different subsystem). I then walked all 12 test methods in the consumer file and all 5 in the worker file under the counterfactual "`UpdateAsync` swallows the residual class and returns an outcome": the only two that throw a non-`NotFound` exception at all are `Dispatch_SetPayloadAsyncThrowsOtherRpcException_…` (asserts `NotThrowAsync` — still passes) and `SweepSignalAsync_OneParentUpdateThrows_…` (asserts `NotThrowAsync` + article-2 `Received(1)` — both still pass). Every other method throws nothing on the residual path. The claim holds. **under (a catch-all would NOT fail test 6, i.e. the new guard is toothless):** `ok` — [existence] test 6 asserts `UpdateAsync` itself throws; a catch-all converting the fifth path into an outcome makes `UpdateAsync` return normally, so the assertion fails. The guard bites. |

### Round-N>1 mandatory row families

| # | Family | Disposition |
|---|---|---|
| F1 | **(a) Fix neighborhood — fix 1: the six new rows A14–A19** and the spec text they restate | `ok` — [totality] each verified independently against code at the tier its claim class demands; full per-row evidence in §1. Restatement check: A14 restates §3's "targeted fault" premise (consistent), A18 restates §3's fault-domain sentence (→ R7), A19 restates §2's retraction (→ R2). No restatement contradicts its row. |
| F2 | **(a) Fix neighborhood — fix 2: the reworded abort message + the new §3 sentence**, and every restatement of the abort's cost | `ok` — [totality] the new message's only new load-bearing claim is "see the preceding per-parent errors for the cause". Both increment sources do emit a preceding per-parent `LogError`: a `Failed` outcome logs at `PopularitySignalConsumer.cs:80-82` before returning at `:83`; a propagating exception logs at `PopularitySignalReconciliationWorker.cs:76-78` inside the same catch that increments. There is no third increment source (R3-over), so the pointer is never dangling. Restatements: "Threshold 5" (six lines / five queries), "Recovery is automatic", and "Out of scope → the abort bounds it at five" all survive the rewording unchanged — none of them named a store, so none was left stale by fix 2. [negative] `grep -n 'engagement store appears' <spec>` → zero hits: the replaced wording survives nowhere in the spec. The new §3 sentence's enumeration → R7. |
| F3 | **(a) Fix neighborhood — fix 3: §2's retraction, the §2 restatement, and new test 6** | `ok` — [totality] internal-consistency sweep of every place the spec speaks about what the tests enforce: §2 "documented, but (until test 6 below) not asserted"; §2 "the contract is documented only in those two test comments; neither test asserts it … Test 6 below closes that gap"; §4 item 6; A1b "documented by `PopularitySignalConsumerTests.cs:411` and `…WorkerTests.cs:173`" (a documentation claim, not an assertion claim); A19 "Neither cited test asserts the propagate-don't-swallow contract". All five agree. No surviving sentence claims the existing suite enforces the contract. → R2, R8. |
| F4 | **(b) Intersected fix texts** — round-1 fix texts whose claims intersect a round-2 discovery | Two intersections, both dispositioned. (i) Round 1's §2.1 fix text asserted the propagating domain is "(Qdrant, collection resolution, api-key minting)", applied verbatim into §3; my producer sweep shows collection resolution (and `KeyToUlong`) cannot throw → `dropped` per R7-over (prose over-inclusion, conclusion unaffected). (ii) Round 1's span item SP2 recorded `RecordingLogger<T>` as existing "twice"; the applied A15 says "three times". [totality] A15 is the correct count: `grep -rn 'class RecordingLogger' --include=*.cs .` → exactly three (`DocumentRerenderQueueWorkerTests.cs:304`, `SchemaRegistryTests.cs:525`, `IntelligenceStoreConsumerTests.cs:2411`). A15 supersedes; `ok`. |
| F5 | **(c) Amendment hunks** | **Empty, stated explicitly.** The single forward-window commit `f18081d` carries the message "applied 3 fixes from 2026-09-13-popularity-sweep-fault-abort-design-critical-review-1 to 2026-09-13-popularity-sweep-fault-abort-design.md", which matches the update skill's in-band commit shape, so no hunk in `git diff 5b29301..HEAD -- <spec>` is out-of-band. Nothing in this family is silently omitted. |

### Data-flow arrows

| # | Arrow → consuming operation | Disposition |
|---|---|---|
| D1 | `UpdateAsync` return value → the sweep's `Failed` test inside the per-row `try` (`PopularitySignalReconciliationWorker.cs:70-73`) | `ok` — [compat] the consuming operation's single parameter is the returned enum, available at the `await` on `:72`. No persistence or serialization boundary is crossed anywhere in this design — the outcome is an in-process return value only, so the in-memory/persisted shape-divergence class does not arise. |
| D2 | `UpdateAsync` return value → consumer call site `PopularitySignalConsumer.cs:217` | `ok` — [compat] the value is discarded. C# emits no diagnostic for an unused awaited result, and no analyzer is promoted to an error → A5. |
| D3 | `UpdateAsync` return value → consumer call site `PopularitySignalConsumer.cs:223` | `ok` — one row per call site, not per operation. Same shape as D2 (the `oldParentKey` reassignment branch), same evidence. [totality] `grep -rn 'updater\.UpdateAsync' --include=*.cs .` (worktrees excluded) → exactly three invocation sites, so no fourth caller sources the value from a different artifact. |
| D4 | counter + `signal` + `SweepInterval` → the abort `LogError`'s three operands | `ok` — [existence] `signal` is `SweepSignalAsync`'s own parameter (`:46`); `SweepInterval` is `private static readonly TimeSpan` on the same class (`:26`); `MaxConsecutiveFailures` is the new sibling const beside `PageSize` (`:27`); `consecutiveFailures` is the design's own local; `SanitizeForLog()` is already applied to `signal.Relation` two lines away at `:78`. |
| D5 | propagating exception → worker `catch (Exception ex)` at `:74` → counter | `ok` — [totality] the catch is unfiltered, so every member of R1's residual class reaches it; R3/R7 disposition the members. Double-counting is structurally impossible: one `await updater.UpdateAsync(...)` per parent either returns an outcome or throws, never both. |
| D6 | test 6's stubs → `UpdateAsync`'s Qdrant write (`PopularitySignalConsumer.cs:125-140`) | `dropped` — [compat] the arrow does not reach its consuming operation as the spec's text literally describes it. Test 6 specifies stubbing only `IVectorWriteService.SetPayloadAsync`; with `IEngagementStoreSearchService.AggregateAsync` unstubbed, NSubstitute returns a completed `Task<AggregationResult?>` carrying `null` (`AggregationResult` is a positional record with no parameterless ctor, so it is not auto-substituted), `UpdateAsync` returns at `:94`, and `SetPayloadAsync` is never called. The spec's own A10 names the missing piece (`StubAggregate` at `PopularitySignalReconciliationWorkerTests.cs:79`; `StubCount` at `PopularitySignalConsumerTests.cs:124`), and the omission cannot produce a vacuous pass — the test asserts a throw that would not occur, so it goes red at first run and the implementer adds the stub. A test sketch that fails loudly at transcription time is a plan-level gap for critical-implementation-review, not a design defect: the asked-for behavior (the sweep abandons; the propagation contract is asserted) is not wrong, broken, or impossible. Fails literal-wrongness. |

---

## 1. Verified-assumptions cross-check

Fresh read of each listed item's cited evidence at HEAD. **All hold.** The six rows added by fix 1 (A14–A19) are checked at the tier their claim class demands, not by spot check.

- **A1** — holds. [existence] `PopularitySignalConsumer.cs:44` is `internal async Task UpdateAsync(`; aggregate `catch` at `:78` with bare `return;` at `:83`; null-result `return;` at `:94`.
- **A1b** — holds. [totality] every terminal point of `:44-141` enumerated (see R1): `:83`, `:94`, end-of-method `:141`, plus a propagating residual closed by construction. Both cited comments exist (`PopularitySignalConsumerTests.cs:409-411`; `…WorkerTests.cs:172-174` — the spec's `:173` lands mid-comment).
- **A1c** — holds. [existence] `:134-141` — the `NotFound` catch logs at `:137-139` and the method body ends at `:141` with no distinct `return`.
- **A2** — holds. [totality] all four cites are one-line inline enums in the owning file: `AuthorizationAction` `Authorization/IRowFieldAuthorizationEvaluator.cs:6`; `EnrichmentKind` `Schema/SchemaDescriptor.cs:107`; `RelationKind` `Schema/SchemaDescriptor.cs:137`; `DocumentSegmentKind` `Schema/DocumentTemplate.cs:7`. None has a dedicated file in `Iverson.Server/`.
- **A3** — holds. [existence] `PopularitySignalReconciliationWorker.cs:65-80` is the `foreach (var row in page)` with the null-tenant `continue` and the per-row `try`/`catch`.
- **A4** — holds. [existence] `:74-79` — `catch (Exception ex)` → `LogError` → falls out and continues the `foreach`. No rethrow.
- **A5** — holds. [negative] `find . -name 'Directory.Build.props' -o -name 'Directory.Build.targets'` (worktrees excluded) → nothing; `grep -rn 'TreatWarningsAsErrors\|AnalysisMode\|EnforceCodeStyleInBuild' --include=*.csproj --include=*.props --include=.editorconfig .` → zero hits outside worktrees.
- **A6** — holds. [totality] `grep -rn 'updater\.UpdateAsync' --include=*.cs .` → exactly three invocation sites: `PopularitySignalConsumer.cs:217`, `:223`, `PopularitySignalReconciliationWorker.cs:72`.
- **A12/A13** — holds. [negative] `internal sealed class PopularitySignalUpdater(` at `:38` with a primary-constructor list and no base list; the exhaustive `\bPopularitySignalUpdater\b` grep shows no interface, delegate, `Func<>` alias, or method-group reference anywhere in `Iverson.Server/` (only the DI registration `Program.cs:308`, the two test constructions, and doc comments).
- **A8** — holds. [totality] all 10 occurrences of `SweepSignalAsync` in `…WorkerTests.cs` — five method names (`:90`, `:116`, `:126`, `:139`, `:162`) and five direct invocations (`:102`, `:119`, `:131`, `:149`, `:181`).
- **A10** — holds. [compat] `StubAggregate` at `…WorkerTests.cs:79-85`; `Task.FromException` throw-simulation at `…WorkerTests.cs:179` and `PopularitySignalConsumerTests.cs:149`/`:397`/`:420`.
- **A11** — holds. [existence] `private const int PageSize = 500;` at `:27`, directly below `private static readonly TimeSpan SweepInterval` at `:26`.
- **A14** *(new — a totality claim over a fault class; checked as one)* — holds. [totality] I read `EngagementRepository.AggregateAsync` end to end (`:369-435`): the only `return null` statements are `:392-394` (`tenantId is null || !TenantIdentifier.IsValid(tenantId)`) and `:404-407` (`catch … when (IsExpectedMissingResourceError(ex))`); every other terminal returns a constructed `AggregationResult` (`:423`, `:428`, `:430-432`). `IsExpectedMissingResourceError` at `:142-148` is exactly the two shapes the row names. Producer sweep for the interface, which the row does not itself perform: `grep -rn 'IEngagementStoreSearchService' --include=*.cs .` → two implementations, and the second (`DisabledEngagementStoreSearchService.cs:22-27`) throws unconditionally, so it also lands on `:78` → `Failed`. The row's "Every other failure propagates to the aggregate `catch` at `:78` → `Failed`" is true for **both** producers. Additionally checked the one way this row could be silently universal in the other direction: `SchemaBuilder.ToEngagementQuerySchema` (`SchemaBuilder.cs:322-323`) passes `d.TypeName` through as `EngagementQuerySchema.TypeName`, and `UpdateAsync` keys its authz dictionary on `childSchema.TypeName` (`:64`), so `authz.GetValueOrDefault(schema.TypeName)` at `:391` resolves — the invalid/absent-tenant branch is not reached for every call.
- **A15** *(new)* — holds. [existence] `NullLogger<PopularitySignalReconciliationWorker>.Instance` is injected at `…WorkerTests.cs:76`, so a recording logger is required. [totality] `grep -rn 'class RecordingLogger' --include=*.cs .` → exactly the three the row names: `DocumentRerenderQueueWorkerTests.cs:304`, `SchemaRegistryTests.cs:525`, `IntelligenceStoreConsumerTests.cs:2411`. The count and all three cites are correct (and supersede round 1's SP2, which said "twice").
- **A16** *(new)* — holds. [negative] `grep -rn '\bPopularityUpdateOutcome\b' --include=*.cs .` (worktrees excluded) → zero hits.
- **A17** *(new)* — holds. [existence] `signal` is the parameter at `PopularitySignalReconciliationWorker.cs:46`; `SweepInterval` is `private static readonly` at `:26`; `MaxConsecutiveFailures` is the new sibling const beside `PageSize` (`:27`); `SanitizeForLog()` is already applied to `signal.Relation` at `:78`.
- **A18** *(new — a totality claim over a fault class)* — holds **as written**, with one refinement that does not falsify it. [totality] `PopularitySignalConsumer.cs:134` is the only catch around the Qdrant write and is filtered to `StatusCode.NotFound`, so every other status propagates; `IntelligenceVectorService.SetPayloadAsync` (`:88-99`) read in full — no `try`, no catch, no retry; `ResolveCollectionName` (`:98`) and `KeyToUlong` (`:99`) are outside the `try`; `MintScopedApiKey` (`:127`) is inside it but the only catch is NotFound-filtered. The row's claim — that the propagating class **includes non-engagement-store members** — is established by Qdrant and `MintScopedApiKey` alone. Refinement (dispositioned at §0 R7-over, not a failure of this row): of the members it lists, `ResolveCollectionName` and `KeyToUlong` are total functions and cannot themselves throw, so they are structural members of the class rather than live producers.
- **A19** *(new — a totality claim over two tests' assertion sets)* — holds. [totality] complete assertion inventory read from source, not sampled. `PopularitySignalConsumerTests.cs:412-430`: body is `RegisterAsync` ×2, `StubCount(1)`, a `_vector.SetPayloadAsync(...).Returns(Task.FromException(...))` stub, a payload literal, a `FetchByKeyAsync` stub, `MakeEvent`, `BuildSut`, an `act` lambda, and exactly **one** assertion at `:428-429`. `…WorkerTests.cs:161-187`: exactly **two** assertions, `:181-182` and `:184-186`. A swallowing catch-all passes all three. Contract text at `:409-411` and `:172-174` is comment, not assertion.
- **(unnumbered) `ConsumerResilience` does not mitigate this today** — holds. [absence] `ConsumerResilience.cs` wraps only `await runConsumers()` in its single `try`; no catch exists anywhere on the path between it and `UpdateAsync`, so nothing the updater swallows can reach it.
- **(unnumbered) established anti-spam pattern** — holds in substance. [existence] `ReconciliationService.ProcessQueuedFailuresAsync` computes `exhaustedCount` at `:68` and emits one `LogWarning` carrying the count at `:70-75`, rather than one line per row. The cite `:65-71` is a line or two short of the full statement; immaterial.

### Span check — design dependencies with no covering assumption

Round 1's five span items SP1–SP5 are now covered by listed items A14–A18 respectively (verified above), so they are no longer gaps. Three dependencies introduced or exposed by the round-1 fixes have no covering assumption; each is verified in-round.

- **SP6 — test 6 must be constructible against existing fixtures.** No listed item covers it (A10 covers failure *simulation* generally, not this test). **Verified in-round, holds:** [compat] `Iverson.Api.csproj:9-12` grants `InternalsVisibleTo("Iverson.Api.Tests")`; `PopularitySignalUpdater` is already constructed directly in both test files (`PopularitySignalConsumerTests.cs:61-62`, `…WorkerTests.cs:72-73`); the exact `Task.FromException(new RpcException(new Status(StatusCode.Unavailable, "down")))` idiom already exists at `PopularitySignalConsumerTests.cs:420`; and all six `UpdateAsync` parameters (`:44-46`) have existing fixtures (`ArticleSchema()`, the signal entry, `CommentSchema()`, `Relations[0]`, a key string, a tenant id). Named residue, dispositioned at §0 D6 and routed to critical-implementation-review rather than left here: the test's text omits the `AggregateAsync` stub needed to reach `SetPayloadAsync`, an omission that fails loudly at first run rather than passing vacuously.
- **SP7 — test 6's "only assertion in the suite" must be true across the whole suite, not just the two cited tests.** A19 covers only the two cited tests. **Verified in-round, holds:** [totality] see §0 R8 — the `\bPopularitySignalUpdater\b` grep bounds the population to two test files, and all 17 test methods in them were walked under the catch-all counterfactual.
- **SP8 — every counter increment must be preceded by a per-parent error line,** which the reworded summary now points the operator at. No listed item covers it. **Verified in-round, holds:** [bidirectional] forward — the two increment sources are `Failed` (logs `LogError` at `PopularitySignalConsumer.cs:80-82` immediately before `return;` at `:83`) and the worker's catch (logs `LogError` at `PopularitySignalReconciliationWorker.cs:76-78` inside the incrementing catch); reverse — there is no third increment source (§0 R3-over enumerates the increment set as exactly those two), so the message's pointer can never dangle.

---

## 2. Literal-wrongness findings

No literal-wrongness findings.

---

## 3. Forced decisions

No forced decisions found.

---

## 4. Previously addressed

- **Round 1 §2.1 — the abort's summary line named a cause the counter cannot determine.** Resolved. The message body is now "consecutive parent-update failures — see the preceding per-parent errors for the cause"; `grep -n 'engagement store appears' <spec>` → zero hits, and the pointer it substitutes is backed by both increment sources (SP8). §3 additionally records why the line cannot name a store.
- **Round 1 §2.2 — "it would break both tests" asserted a guardrail that did not exist.** Resolved on both halves: §2's claim is retracted and replaced with the true statement ("neither test asserts it, so a catch-all would pass the suite unchanged"), independently re-derived at §0 R2; and §4 test 6 adds the missing assertion, whose bite and uniqueness are confirmed at §0 R8.
- **Round 1 §1 span items SP1–SP5** — all five promoted into the spec's `Verified assumptions` section as A14–A18 and reconfirmed above. SP2's undercount of `RecordingLogger<T>` ("twice") was corrected to three by A15.

---

## 5. Recommendation

✅ **Approve as-is** — §2 and §3 are both empty. The spec is ready for implementation planning.

The three round-1 fixes were applied without introducing a new defect and without leaving a stale restatement: the internal-consistency sweep across every place the spec speaks about what the tests enforce (§2 twice, §4 item 6, A1b, A19) found all five in agreement, and the replaced wording survives nowhere. The six new `Verified assumptions` rows were checked at the tier their claim class demands rather than by spot check — A14's "exactly two conditions" against a full read of `AggregateAsync` plus a producer sweep that found and dispositioned the second `IEngagementStoreSearchService` implementation, A18's fault-class claim against every catch and every throwing call on the write path, A19's "exactly one / exactly two assertions" against complete inventories of both test bodies — and all six hold. Test 6's totality claim over the whole suite was closed by bounding the population with a type-level grep and walking all 17 test methods in it. Two imprecisions were found and both fail the literal-wrongness test: §3's parenthetical lists two propagating "domains" that are total functions and cannot throw (§0 R7, conclusion unaffected), and test 6's text omits the aggregate stub needed to reach `SetPayloadAsync` (§0 D6, fails loudly at transcription — a critical-implementation-review item).
