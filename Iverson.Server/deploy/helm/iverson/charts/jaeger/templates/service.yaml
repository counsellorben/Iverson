apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-jaeger
spec:
  selector:
    app: {{ .Release.Name }}-jaeger
  ports:
    - name: ui
      port: 16686
    - name: otlp-grpc
      port: 4317
