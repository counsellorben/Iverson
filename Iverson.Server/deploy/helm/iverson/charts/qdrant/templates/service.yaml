apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-qdrant
spec:
  clusterIP: None
  selector:
    app: {{ .Release.Name }}-qdrant
  ports:
    - name: rest
      port: 6333
    - name: grpc
      port: 6334
    - name: raft
      port: 6335
