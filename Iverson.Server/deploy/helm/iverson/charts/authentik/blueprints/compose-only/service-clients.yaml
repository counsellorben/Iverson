version: 1
metadata:
  name: Service/automation OAuth2 clients and human-provider correction (docker-compose dev values)
#
# ── SECURITY NOTE: LOCAL-ONLY DEVELOPMENT ────────────────────────────────────
#
# This file contains intentionally static, hardcoded OAuth2 client secrets and
# default user passwords. These values are committed to source control for
# local-development convenience ONLY.
#
# THIS DOCKER-COMPOSE STACK MUST NEVER BE EXPOSED BEYOND LOCALHOST OR USED IN:
#   - Shared/collaborative environments
#   - CI/CD pipelines
#   - Production or production-like (staging, test) deployments
#   - Any network-accessible endpoint
#
# If deployment requirements ever change such that this compose target must be
# accessible beyond a single developer's local machine, immediately stop and
# rotate these values. Secrets must be generated at deploy-time instead, using
# the same pattern as deploy/helm/iverson/charts/authentik/templates/secret-service-clients.yaml.
#
entries:
  - model: authentik_providers_oauth2.scopemapping
    identifiers:
      scope_name: admin
    attrs:
      name: "Iverson: admin operator scope"
      description: Grants operator-tier access to Iverson's admin endpoints
      expression: "return {}"
  - model: authentik_providers_oauth2.scopemapping
    identifiers:
      scope_name: schema_admin
    attrs:
      name: "Iverson: schema admin scope"
      description: Grants rights to register/redefine entity schemas
      expression: "return {}"
  - model: authentik_providers_oauth2.scopemapping
    identifiers:
      scope_name: groups
    attrs:
      name: "Iverson: group membership"
      description: Exposes the authenticated user's Authentik group names
      expression: "return [group.name for group in user.ak_groups.all()]"
  - model: authentik_providers_oauth2.scopemapping
    identifiers:
      scope_name: tenant_id
    attrs:
      name: "Iverson: tenant id"
      description: The caller's tenant identifier for data isolation
      expression: 'return {"tenant_id": request.user.attributes.get("tenant_id")}'
  - model: authentik_providers_oauth2.scopemapping
    identifiers:
      scope_name: tenant_id_loadtest
    attrs:
      name: "Iverson: loadtest tenant id"
      description: Fixed tenant identifier issued to the loadtest service client
      expression: 'return {"tenant_id": "tenant-loadtest"}'
  - model: authentik_providers_oauth2.scopemapping
    identifiers:
      scope_name: tenant_id_webtest
    attrs:
      name: "Iverson: webtest tenant id"
      description: Fixed tenant identifier issued to the webtest service client
      expression: 'return {"tenant_id": "tenant-webtest"}'
  - model: authentik_providers_oauth2.scopemapping
    identifiers:
      scope_name: tenant_id_admin
    attrs:
      name: "Iverson: admin automation tenant id"
      description: Fixed tenant identifier issued to the admin-automation service client
      expression: 'return {"tenant_id": "tenant-admin"}'
  - model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: iverson-loadtest
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_type: confidential
      client_id: "dev-iverson-loadtest-client-id"
      client_secret: "dev-only-not-for-production-loadtest-secret-0123456789"
      redirect_uris: []
      grant_types: ["client_credentials"]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      issuer_mode: global
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, schema_admin]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, tenant_id_loadtest]]
  - model: authentik_core.application
    identifiers:
      slug: iverson-loadtest
    attrs:
      name: iverson-loadtest
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, iverson-loadtest]]
  - model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: iverson-webtest
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_type: confidential
      client_id: "dev-iverson-webtest-client-id"
      client_secret: "dev-only-not-for-production-webtest-secret-0123456789"
      redirect_uris: []
      grant_types: ["client_credentials"]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      issuer_mode: global
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, tenant_id_webtest]]
  - model: authentik_core.application
    identifiers:
      slug: iverson-webtest
    attrs:
      name: iverson-webtest
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, iverson-webtest]]
  - model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: iverson-admin-automation
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_type: confidential
      client_id: "dev-iverson-admin-automation-client-id"
      client_secret: "dev-only-not-for-production-admin-secret-0123456789"
      redirect_uris: []
      grant_types: ["client_credentials"]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      issuer_mode: global
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, admin]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, schema_admin]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, tenant_id_admin]]
  - model: authentik_core.application
    identifiers:
      slug: iverson-admin-automation
    attrs:
      name: iverson-admin-automation
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, iverson-admin-automation]]
  - model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: iverson-oidc-default
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_type: public
      redirect_uris:
        - matching_mode: strict
          url: "http://localhost/placeholder-callback"
          redirect_uri_type: authorization
        - matching_mode: strict
          url: "http://localhost:5173/callback"
          redirect_uri_type: authorization
      client_id: "dev-iverson-human-oidc-client-id"
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      issuer_mode: global
      grant_types: ["authorization_code", "refresh_token"]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, groups]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, tenant_id]]
  - model: authentik_core.application
    identifiers:
      slug: iverson-api
    attrs:
      name: iverson-api
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, iverson-oidc-default]]
  - model: authentik_brands.brand
    identifiers:
      domain: authentik-default
    attrs:
      branding_title: "Iverson"
      branding_custom_css: |
        :root, html[data-theme=dark] {
          --pf-global--primary-color--100: #006BB6;
          --pf-global--primary-color--200: #006BB6;
          --pf-global--primary-color--300: #006BB6;
          --pf-global--primary-color--dark-100: #006BB6;
          --pf-global--primary-color--light-100: #006BB6;
          --pf-global--secondary-color--100: #ED174C;
          --pf-global--BackgroundColor--100: #141414;
          --pf-global--BackgroundColor--light-100: #141414;
          --pf-global--BackgroundColor--dark-100: #141414;
          --ak-c-login__main--Color: #F5F5F5;
          --pf-global--Color--100: #F5F5F5;
          --ak-dark-background: #010101;
          --ak-dark-background-light: #0A0A0A;
          --ak-dark-background-lighter: #141414;
          --ak-font-family-sans-serif: 'Fraunces', serif;
          --ak-generic-display: 'Fraunces', serif;
        }
        /* Fraunces isn't installed on this origin by default (it's self-hosted separately
           by the admin UI's own bundle) -- @font-face registers it document-wide, including
           for the shadow-DOM flow-executor, since font registration isn't shadow-scoped. */
        @font-face {
          font-family: 'Fraunces';
          font-weight: 900;
          font-style: normal;
          font-display: swap;
          src: url('/static/dist/assets/fonts/Fraunces/fraunces-900.woff2') format('woff2');
        }
        /* branding_logo/branding_default_flow_background are validated FileFields that
           reject arbitrary strings (including data: URIs), so the stock authentik logo
           and background photo are removed here via CSS instead of the Brand model fields. */
        body {
          background-color: #010101 !important;
        }
        body::before {
          background-image: none !important;
        }
        ak-flow-executor::part(branding) {
          display: none !important;
        }
  - model: authentik_flows.flow
    identifiers:
      slug: default-authentication-flow
    attrs:
      title: "Welcome to Iverson!"
  - model: authentik_flows.flow
    identifiers:
      slug: default-source-authentication
    attrs:
      title: "Welcome to Iverson!"
  - model: authentik_flows.flow
    identifiers:
      slug: default-source-enrollment
    attrs:
      title: "Welcome to Iverson! Please select a username."
  - model: authentik_flows.flow
    identifiers:
      slug: initial-setup
    attrs:
      title: "Welcome to Iverson!"
  - model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: iverson-loadtest-human
    attrs:
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_type: public
      redirect_uris:
        - matching_mode: strict
          url: "http://localhost/placeholder-callback"
          redirect_uri_type: authorization
      client_id: "dev-iverson-loadtest-human-client-id"
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      issuer_mode: global
      grant_types: ["authorization_code", "refresh_token"]
      access_token_validity: "hours=2"
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, groups]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, tenant_id]]
  - model: authentik_core.application
    identifiers:
      slug: iverson-loadtest-human
    attrs:
      name: iverson-loadtest-human
      provider: !Find [authentik_providers_oauth2.oauth2provider, [name, iverson-loadtest-human]]
  - model: authentik_core.user
    identifiers:
      username: iverson-acting-user-smoke-test
    attrs:
      username: iverson-acting-user-smoke-test
      name: "Iverson Acting-User Smoke Test"
      email: iverson-acting-user-smoke-test@example.invalid
      password: "dev-only-not-for-production-smoke-test-password-0123456789"
      is_active: true
      attributes:
        tenant_id: "tenant-smoke-test"
  - model: authentik_core.group
    identifiers:
      name: iverson-loadtest-bypass
    attrs: {}
  - model: authentik_core.user
    identifiers:
      username: iverson-loadtest-bypass-user
    attrs:
      username: iverson-loadtest-bypass-user
      name: "Iverson LoadTest Bypass"
      email: iverson-loadtest-bypass-user@example.invalid
      password: "dev-only-not-for-production-bypass-password-0123456789"
      is_active: true
      attributes:
        tenant_id: "tenant-bypass"
      groups:
        - !Find [authentik_core.group, [name, iverson-loadtest-bypass]]
  - model: authentik_core.group
    identifiers:
      name: tenant-admins
    attrs: {}
  - model: authentik_core.user
    identifiers:
      username: iverson-admin-orchestrator
    attrs:
      username: iverson-admin-orchestrator
      name: "Iverson Admin Orchestrator"
      email: iverson-admin-orchestrator@example.invalid
      password: "dev-only-not-for-production-admin-orchestrator-password"
      is_active: true
      is_superuser: true
  - model: authentik_core.token
    identifiers:
      identifier: iverson-admin-orchestrator-token
    attrs:
      key: "dev-only-not-for-production-admin-orchestrator-token"
      user: !Find [authentik_core.user, [username, iverson-admin-orchestrator]]
      intent: api
