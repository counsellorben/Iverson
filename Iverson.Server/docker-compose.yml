# ────────────────────────────────────────────────────────────────────────────
# SECURITY NOTE: LOCAL-ONLY DEVELOPMENT
# ────────────────────────────────────────────────────────────────────────────
#
# This file defines a complete Iverson stack for local development, including
# several services with intentionally static, hardcoded default credentials:
#   - PostgreSQL: user/password = iverson/iverson
#   - Authentik: admin password = dev-admin-password, bootstrap token = dev-bootstrap-token-0123456789
#   - Service OAuth2 client secrets (see deploy/helm/iverson/charts/authentik/blueprints/compose-only/service-clients.yaml)
#   - Qdrant: API key = dev-only-not-for-production-qdrant-key-0123456789 (shared by the qdrant
#     and iverson-api services below)
#   - And other dev-convenience values marked dev-only-not-for-production
#
# THIS DOCKER-COMPOSE STACK MUST NEVER BE EXPOSED BEYOND LOCALHOST OR USED IN:
#   - Shared/collaborative environments
#   - CI/CD pipelines
#   - Production or production-like (staging, test) deployments
#   - Any network-accessible endpoint
#
# For deployment beyond a local developer's machine, use Kubernetes (deploy/kind/ or deploy/terraform/ + deploy/helm/).
# All Kubernetes deployment targets generate real per-release secrets at deploy-time and never commit static credentials.
#
# ────────────────────────────────────────────────────────────────────────────

services:

  # ── Infrastructure ──────────────────────────────────────────────────────────

  postgres:
    image: postgres:16
    container_name: iverson-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: iverson
      POSTGRES_PASSWORD: iverson
      POSTGRES_DB: iverson
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./deploy/postgres/init-authentik-db.sql:/docker-entrypoint-initdb.d/init-authentik-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U iverson"]
      interval: 10s
      timeout: 5s
      retries: 5

  starrocks:
    image: starrocks/allin1-ubuntu:4.1.1
    container_name: iverson-starrocks
    restart: unless-stopped
    ports:
      - "8030:8030"   # FE HTTP / admin
      - "9030:9030"   # FE MySQL query port
    volumes:
      - starrocks_data:/data/deploy
    healthcheck:
      test: ["CMD-SHELL", "mysql -h 127.0.0.1 -P 9030 -u root --connect-timeout=3 -e 'SELECT 1' 2>/dev/null || exit 1"]
      interval: 20s
      timeout: 10s
      retries: 15
      start_period: 60s

  qdrant:
    image: qdrant/qdrant:v1.18.2
    container_name: iverson-qdrant
    restart: unless-stopped
    environment:
      QDRANT__SERVICE__API_KEY: "dev-only-not-for-production-qdrant-key-0123456789"
      QDRANT__SERVICE__JWT_RBAC: "true"
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/localhost/6333'"]
      interval: 10s
      timeout: 5s
      retries: 5

  ollama:
    image: ollama/ollama:0.5.0
    container_name: iverson-ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 10

  ollama-init:
    image: curlimages/curl:8.18.0
    container_name: iverson-ollama-init
    depends_on:
      ollama:
        condition: service_healthy
    entrypoint: ["sh", "-c"]
    command:
      - |
        curl -X POST http://ollama:11434/api/pull -d '{"name":"nomic-embed-text"}' &&
        curl -X POST http://ollama:11434/api/pull -d '{"name":"qwen2.5:3b"}'
    restart: "no"

  zookeeper:
    image: confluentinc/cp-zookeeper:7.6.0
    container_name: iverson-zookeeper
    restart: unless-stopped
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    volumes:
      - zookeeper_data:/var/lib/zookeeper/data
      - zookeeper_logs:/var/lib/zookeeper/log

  kafka:
    image: confluentinc/cp-kafka:7.6.0
    container_name: iverson-kafka
    init: true
    restart: unless-stopped
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092,PLAINTEXT_INTERNAL://kafka:29092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_INTERNAL:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT_INTERNAL
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_NUM_PARTITIONS: 12
    volumes:
      - kafka_data:/var/lib/kafka/data
    healthcheck:
      test: ["CMD-SHELL", "kafka-broker-api-versions --bootstrap-server localhost:9092 || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10

  # ── Observability ────────────────────────────────────────────────────────────

  jaeger:
    image: jaegertracing/all-in-one:1.62.0
    container_name: iverson-jaeger
    restart: unless-stopped
    environment:
      - COLLECTOR_OTLP_ENABLED=true
    ports:
      - "16686:16686"  # Jaeger UI
      - "4317:4317"    # OTLP gRPC (traces + logs)
      - "4318:4318"    # OTLP HTTP
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:14269/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  prometheus:
    image: prom/prometheus:v2.55.1
    container_name: iverson-prometheus
    restart: unless-stopped
    volumes:
      - ./deploy/prometheus/prometheus.local.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9090/-/ready || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Identity ────────────────────────────────────────────────────────────────

  redis:
    image: redis:7.4-alpine
    container_name: iverson-redis
    restart: unless-stopped
    command: ["redis-server", "--save", ""]   # cache-only, no volume mounted — matches the Helm
                                               # subchart's identical reasoning (Task 1)
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  authentik-server:
    image: ghcr.io/goauthentik/server:2026.5.3
    container_name: iverson-authentik-server
    restart: unless-stopped
    command: server
    environment:
      AUTHENTIK_SECRET_KEY: "dev-only-not-for-production-use-0123456789abcdef"
      AUTHENTIK_REDIS__HOST: redis
      AUTHENTIK_POSTGRESQL__HOST: postgres
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_POSTGRESQL__USER: authentik
      AUTHENTIK_POSTGRESQL__PASSWORD: authentik
      AUTHENTIK_BOOTSTRAP_EMAIL: admin@iverson.local
      AUTHENTIK_BOOTSTRAP_PASSWORD: dev-admin-password
      AUTHENTIK_BOOTSTRAP_TOKEN: dev-bootstrap-token-0123456789
    ports:
      - "9000:9000"
    volumes:
      - ./deploy/helm/iverson/charts/authentik/blueprints:/blueprints/custom:ro
      - ./deploy/helm/iverson/charts/authentik/assets/fonts/fraunces-900.woff2:/web/dist/assets/fonts/Fraunces/fraunces-900.woff2:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "ak", "healthcheck"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s

  authentik-worker:
    image: ghcr.io/goauthentik/server:2026.5.3
    container_name: iverson-authentik-worker
    restart: unless-stopped
    command: worker
    environment:
      AUTHENTIK_SECRET_KEY: "dev-only-not-for-production-use-0123456789abcdef"
      AUTHENTIK_REDIS__HOST: redis
      AUTHENTIK_POSTGRESQL__HOST: postgres
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_POSTGRESQL__USER: authentik
      AUTHENTIK_POSTGRESQL__PASSWORD: authentik
      AUTHENTIK_BOOTSTRAP_EMAIL: admin@iverson.local
      AUTHENTIK_BOOTSTRAP_PASSWORD: dev-admin-password
      AUTHENTIK_BOOTSTRAP_TOKEN: dev-bootstrap-token-0123456789
    volumes:
      - ./deploy/helm/iverson/charts/authentik/blueprints:/blueprints/custom:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "ak", "healthcheck"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s

  # ── Application ─────────────────────────────────────────────────────────────

  iverson-api:
    build:
      context: ..
      dockerfile: Iverson.Server/Iverson.Api/Dockerfile
    image: iverson-api
    container_name: iverson-api
    restart: unless-stopped
    user: "1000:1000"
    ports:
      - "8080:8080"
      - "8081:8081"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - Otel__Endpoint=http://jaeger:4317
      - Embeddings__BaseUrl=http://ollama:11434
      - Embeddings__ModelId=nomic-embed-text
      - Enrichment__BaseUrl=http://ollama:11434
      - Enrichment__ModelId=qwen2.5:3b
      - Kafka__BootstrapServers=kafka:29092
      - Qdrant__ApiKey=dev-only-not-for-production-qdrant-key-0123456789
      - Authentication__Authority=http://authentik-server:9000/application/o/iverson-api/
      - Authentication__ExternalIssuer=http://authentik.iverson.local/application/o/iverson-api/
      - Authentication__ValidAudiences__0=dev-iverson-human-oidc-client-id
      - Authentication__ValidAudiences__1=dev-iverson-loadtest-client-id
      - Authentication__ValidAudiences__2=dev-iverson-webtest-client-id
      - Authentication__ValidAudiences__3=dev-iverson-admin-automation-client-id
      # The LoadTest's data-plane calls (Post/Get/Search) carry the tenant-admin *user* token as
      # their service credential — AddIversonClient routes the persistence/retrieval/search stubs
      # through dataPlaneTokenProvider, leaving only the mapping stub on client credentials. That
      # token is minted via the human OIDC client below, so without this audience every Post fails
      # the default scheme with a bare HTTP 401 while RegisterSchema still succeeds.
      - Authentication__ValidAudiences__4=dev-iverson-loadtest-human-client-id
      - Authentication__ActingUser__Authority=http://authentik-server:9000/application/o/iverson-api/
      - Authentication__ActingUser__ValidAudiences__0=dev-iverson-loadtest-human-client-id
      - Authentik__BaseUrl=http://authentik-server:9000
      - Authentik__AdminToken=dev-only-not-for-production-admin-orchestrator-token
      - Jaeger__OtlpHttpUrl=http://jaeger:4318
    # Kestrel does not begin listening until Program.cs's startup block has finished — the
    # bootstrap DDL (EnsureRuntimeRoleAsync, the plumbing-table EnsureTableAsync calls, and every
    # ApplySchemaAsync) is awaited BEFORE app.Run(). A successful TCP connect to 8080 is therefore
    # a precise "this replica's bootstrap DDL is complete" signal, which is exactly what
    # iverson-worker's depends_on needs. A bash /dev/tcp redirect rather than curl/wget: the
    # aspnet:10.0 runtime image ships neither (verified), but does ship bash.
    healthcheck:
      # `bash -c`, not CMD-SHELL: CMD-SHELL runs /bin/sh, which on this image is dash, and dash
      # has no /dev/tcp support at all ("cannot create /dev/tcp/...: Directory nonexistent").
      test: ["CMD", "bash", "-c", "exec 3<>/dev/tcp/127.0.0.1/8080"]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 20s
    depends_on:
      postgres:
        condition: service_healthy
      starrocks:
        condition: service_healthy
      qdrant:
        condition: service_healthy
      kafka:
        condition: service_healthy
      jaeger:
        condition: service_healthy
      authentik-server:
        condition: service_healthy
      ollama-init:
        condition: service_completed_successfully

  # The worker role. Every Kafka consumer and background reconciliation worker in Iverson.Api is
  # gated on `workloadRole == "worker"` (Iverson.Api/Program.cs) — IntelligenceStoreConsumer,
  # DocumentRerenderConsumer, the DLQ/reconciliation workers, and AddEngagementStoreConsumer's
  # hosted consumer. Without this service NO asynchronous projection is ever populated: Qdrant
  # stays empty, StarRocks stays empty, and every read served from a projection returns nothing
  # while the write path looks perfectly healthy. The Helm chart has always deployed a worker
  # (deploy/helm/iverson/charts/worker); compose was the outlier.
  iverson-worker:
    build:
      context: ..
      dockerfile: Iverson.Server/Iverson.Api/Dockerfile
    image: iverson-api
    container_name: iverson-worker
    restart: unless-stopped
    user: "1000:1000"
    # No `ports:` on purpose, mirroring the chart's worker Deployment, which publishes no gRPC
    # port either: this role serves no client traffic. Nothing is published to the host, so the
    # container's own 8080/8081 cannot collide with iverson-api's published bindings.
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      # The one line that distinguishes this service from iverson-api. The env-var configuration
      # provider folds "__" into ":" but leaves a single underscore alone, so this lands in
      # IConfiguration under the literal key "WORKLOAD_ROLE" — the first key Program.cs checks.
      - WORKLOAD_ROLE=worker
      - Otel__Endpoint=http://jaeger:4317
      - Embeddings__BaseUrl=http://ollama:11434
      - Embeddings__ModelId=nomic-embed-text
      - Enrichment__BaseUrl=http://ollama:11434
      - Enrichment__ModelId=qwen2.5:3b
      - Kafka__BootstrapServers=kafka:29092
      - Qdrant__ApiKey=dev-only-not-for-production-qdrant-key-0123456789
      - Authentication__Authority=http://authentik-server:9000/application/o/iverson-api/
      - Authentication__ExternalIssuer=http://authentik.iverson.local/application/o/iverson-api/
      - Authentication__ValidAudiences__0=dev-iverson-human-oidc-client-id
      - Authentication__ValidAudiences__1=dev-iverson-loadtest-client-id
      - Authentication__ValidAudiences__2=dev-iverson-webtest-client-id
      - Authentication__ValidAudiences__3=dev-iverson-admin-automation-client-id
      - Authentication__ValidAudiences__4=dev-iverson-loadtest-human-client-id
      - Authentication__ActingUser__Authority=http://authentik-server:9000/application/o/iverson-api/
      - Authentication__ActingUser__ValidAudiences__0=dev-iverson-loadtest-human-client-id
      - Authentik__BaseUrl=http://authentik-server:9000
      - Authentik__AdminToken=dev-only-not-for-production-admin-orchestrator-token
      - Jaeger__OtlpHttpUrl=http://jaeger:4318
    depends_on:
      # The same backing-service conditions iverson-api uses...
      postgres:
        condition: service_healthy
      starrocks:
        condition: service_healthy
      qdrant:
        condition: service_healthy
      kafka:
        condition: service_healthy
      jaeger:
        condition: service_healthy
      authentik-server:
        condition: service_healthy
      ollama-init:
        condition: service_completed_successfully
      # ...plus one more: wait for iverson-api to finish its OWN bootstrap before starting this
      # one. Both roles run the identical startup block (it sits outside Program.cs's
      # `workloadRole == "api"` guard), and that block is genuinely concurrency-safe by design —
      # every statement is CREATE/ALTER ... IF [NOT] EXISTS or ON CONFLICT DO NOTHING, and the two
      # CREATE-then-catch paths (EnsureRuntimeRoleAsync, the RLS policy) both swallow SQLSTATE
      # 42710 with an explicit "another replica created it concurrently" comment. Helm already
      # runs api x2 plus a worker against one database on that basis.
      #
      # It is sequenced anyway because "safe by design" is not the same as "raced on every single
      # `docker compose up -d`". Bare CREATE TABLE/INDEX IF NOT EXISTS is still not atomic against
      # an identical concurrent statement in Postgres — it can surface as 23505 on a pg_catalog
      # unique index, and that SQLSTATE is not caught anywhere. Starting second closes the window
      # completely rather than relying on losing a race that is cheap to avoid: by the time this
      # container starts, every IF NOT EXISTS check it runs is a no-op.
      iverson-api:
        condition: service_healthy

volumes:
  postgres_data:
  starrocks_data:
  qdrant_data:
  zookeeper_data:
  zookeeper_logs:
  kafka_data:
  ollama_data:
  prometheus_data:
